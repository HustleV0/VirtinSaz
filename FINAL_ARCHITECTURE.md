# 🗺️ راهنمای جامع معماری و ساختار پلتفرم سایت‌ساز (Site Builder)

این سند شامل جزئیات معماری، ساختار دیتابیس و استراتژی پیاده‌سازی پلتفرم است که به صورت بهینه برای مدیریت انواع مختلف سایت‌ها (رستوران، بلاگ، فروشگاه و ...) طراحی شده است.

---

## 🎯 ۱. معماری کلی سیستم: Modular Monolith
به جای ساخت چندین بک‌اِند جداگانه، از یک سیستم مرکزی واحد استفاده می‌کنیم که به صورت ماژولار طراحی شده است.

### **چرا این رویکرد؟**
*   **یکپارچگی:** مدیریت کاربران، پرداخت‌ها و تنظیمات کلی در یک نقطه انجام می‌شود.
*   **هزینه کم:** نگهداری یک دیتابیس و یک سرور به جای چندین زیرساخت جداگانه.
*   **توسعه سریع:** افزودن یک نوع سایت جدید (مثلاً سایت شرکتی) فقط با افزودن یک ماژول جدید به سیستم فعلی ممکن است.
*   **اشتراک منابع:** کاربران می‌توانند چندین سایت از انواع مختلف را با یک حساب کاربری مدیریت کنند.

---

## ⚖️ ۲. انتخاب تکنولوژی بک‌اِند (Backend)
پس از بررسی گزینه‌های مختلف، **Django** به عنوان بهترین گزینه برای این پروژه انتخاب شد.

| فریمورک | وضعیت برای این پروژه | دلیل انتخاب/رد |
| :--- | :--- | :--- |
| **Django** | ✅ **منتخب** | سرعت توسعه بسیار بالا، ORM قدرتمند برای روابط پیچیده، پنل ادمین آماده برای مدیریت کل سیستم. |
| **FastAPI** | ⚠️ جایگزین | سرعت اجرای بالا اما نیاز به کدنویسی دستی بسیار زیاد برای احراز هویت و پنل ادمین. |
| **Node.js** | ⚠️ جایگزین | مناسب برای سیستم‌های Real-time اما مدیریت روابط پیچیده دیتابیس در آن به قدرت جنگو نیست. |

**نتیجه:** برای یک پروژه "سایت‌ساز" که بیشتر عملیات‌ها از نوع CRUD (ثبت و ویرایش داده) است، جنگو در زمان و هزینه‌های شما صرفه‌جویی بزرگی می‌کند.

---

## 💾 ۳. ساختار دیتابیس (Multi-Tenancy)
ما از استراتژی **دیتابیس مشترک با تفکیک داده (Discriminator Pattern)** استفاده می‌کنیم.

### **جداول پایه (Core Tables):**
این جداول بین تمام سایت‌ها مشترک هستند:
*   **کاربران:** اطلاعات ورود و پروفایل.
*   **سایت‌ها:** شامل نام سایت، ساب‌دومین، نوع سایت (رستوران/بلاگ) و تنظیمات کلی.
*   **صفحات:** ساختار صفحات ساخته شده توسط کاربر.
*   **رسانه‌ها:** کتابخانه تصاویر و فایل‌های آپلود شده.

### **جداول اختصاصی (Feature-Specific):**
هر ماژول جداول خاص خود را دارد که با یک کلید (`site_id`) به سایت مربوطه متصل می‌شوند:
*   **ماژول بلاگ:** جداول پست‌ها، دسته‌بندی‌ها و نظرات.
*   **ماژول رستوران:** جداول منوی غذا، قیمت‌ها و دسته‌بندی منو.
*   **ماژول فروشگاه:** جداول محصولات، سفارشات و انبارداری.

---

## 🎨 ۴. ساختار فرانت‌اِند و داشبورد
استفاده از **Next.js** برای داشبورد مدیریت و نمایش سایت‌ها.

### **داشبورد تطبیق‌پذیر (Adaptive Dashboard):**
کاربر فقط یک داشبورد می‌بیند، اما محتوای آن بر اساس نوع سایت تغییر می‌کند:
1.  اگر سایت **رستوران** باشد: منوی "مدیریت غذا" و "QR کد" فعال می‌شود.
2.  اگر سایت **بلاگ** باشد: منوی "نوشتن پست" و "نظرات" نمایش داده می‌شود.

### **سیستم صفحه‌ساز (Page Builder):**
*   داده‌های چیدمان صفحات به صورت **JSON** ذخیره می‌شوند.
*   این کار اجازه می‌دهد بدون تغییر در ساختار دیتابیس، کاربر بتواند بلوک‌های مختلف (متن، عکس، فرم) را در صفحه جابجا کند.

---

## 🚀 ۵. نقشه راه پیاده‌سازی (Roadmap)

1.  **فاز اول (پایه):** راه‌اندازی جنگو، سیستم ثبت‌نام کاربران و قابلیت ساخت یک سایت خام.
2.  **فاز دوم (اولین ماژول):** پیاده‌سازی کامل بخش رستوران (ساخت منو و QR کد) به دلیل سادگی و تقاضای بالا.
3.  **فاز سوم (داشبورد):** ساخت پنل مدیریت در Next.js که به API جنگو متصل شود.
4.  **فاز چهارم (صفحه‌ساز):** طراحی سیستم Drag & Drop برای ویرایش ظاهر سایت‌ها.
5.  **فاز پنجم (توسعه):** افزودن ماژول‌های بلاگ، فروشگاه و سیستم پرداخت.

---

## 🛠️ نکات کلیدی برای موفقیت:
*   **امنیت:** در هر کوئری دیتابیس، همیشه داده‌ها را بر اساس `site_id` فیلتر کنید تا هیچ کاربری به داده‌های سایت دیگر دسترسی نداشته باشد.
*   **مقیاس‌پذیری:** از همان ابتدا برای فایل‌های آپلودی از سرویس‌های ابری (مانند S3) استفاده کنید.
*   **انعطاف:** تنظیمات ظاهری (رنگ، فونت) را در فیلدهای JSON ذخیره کنید تا دست کاربر در شخصی‌سازی باز باشد.
