# ✅ چک‌لیست مراحل پیاده‌سازی پلتفرم سایت‌ساز

این چک‌لیست بر اساس بررسی فرانت‌اِند موجود و سند معماری `FINAL_ARCHITECTURE.md` تهیه شده است.

---

## 🏗️ فاز ۱: زیرساخت بک‌اِند (Django)
- [ ] **راه‌اندازی پروژه Django:**
    - [ ] نصب Django و Django Rest Framework (DRF).
    - [ ] تنظیمات PostgreSQL و اتصال به دیتابیس.
    - [ ] کانفیگ Docker برای محیط توسعه.
- [ ] **مدل‌سازی دیتابیس (Multi-Tenancy):**
    - [ ] ایجاد مدل `User` سفارشی.
    - [ ] ایجاد مدل `Site` (شامل ساب‌دومین، نوع سایت و تنظیمات JSON).
    - [ ] ایجاد مدل‌های پایه برای `Page` و `Media`.
- [ ] **سیستم احراز هویت (Auth):**
    - [ ] پیاده‌سازی JWT Authentication.
    - [ ] APIهای ثبت‌نام، ورود و تایید ایمیل/شماره.

---

## 🍽️ فاز ۲: ماژول اختصاصی رستوران (اولین فاز عملیاتی)
- [ ] **مدل‌های رستوران:**
    - [ ] مدل `Category` (دسته بندی غذاها).
    - [ ] مدل `Product` (غذاها، قیمت‌ها، تصاویر).
- [ ] **APIهای رستوران:**
    - [ ] CRUD کامل برای دسته‌بندی‌ها و محصولات.
    - [ ] API عمومی برای نمایش منو در بخش Preview بر اساس `slug`.
- [ ] **تولید QR کد:**
    - [ ] ایجاد سرویس تولید QR کد داینامیک برای هر سایت.

---

## 💻 فاز ۳: اتصال فرانت‌اِند به بک‌اِند
- [ ] **یکپارچه‌سازی API:**
    - [ ] تنظیم `Axios` یا `Fetch` در Next.js برای ارتباط با جنگو.
    - [ ] جایگزینی `mock-data.ts` با داده‌های دریافتی از سرور در داشبورد.
    - [ ] پیاده‌سازی مدیریت State (مانند React Query یا SWR) برای کش کردن داده‌ها.
- [ ] **مدیریت تصاویر:**
    - [ ] اتصال بخش آپلود تصاویر به Django Media و در نهایت S3.

---

## 🎨 فاز ۴: صفحه‌ساز و شخصی‌سازی (Advanced)
- [ ] **سیستم Layout داینامیک:**
    - [ ] ذخیره و بازیابی چیدمان بخش‌های مختلف صفحه از فیلد JSON در دیتابیس.
    - [ ] ایجاد قابلیت تغییر تم و رنگ‌بندی به صورت Real-time در داشبورد.
- [ ] **داشبورد تطبیق‌پذیر:**
    - [ ] نمایش منوهای داشبورد بر اساس نوع سایت (`site_type`).

---

## 🚀 فاز ۵: استقرار و بهینه‌سازی
- [ ] **مدیریت دامنه‌ها:**
    - [ ] تنظیمات Nginx برای هندل کردن Wildcard Subdomains.
- [ ] **امنیت و کارایی:**
    - [ ] فیلتر کردن تمام کوئری‌ها بر اساس `site_id`.
    - [ ] بهینه‌سازی سرعت لود تصاویر.
