🎯 معماری کلی: Single Backend با Multi-Tenant Database
چرا این رویکرد؟

یک بک‌اند Django مرکزی با قابلیت مدیریت انواع مختلف سایت‌ها
یک دیتابیس اصلی PostgreSQL با معماری Multi-Tenant
ماژولار بودن برای افزودن ویژگی‌های خاص هر نوع سایت
یک داشبورد واحد با قابلیت‌های Dynamic بر اساس نوع سایت


🏛️ ساختار دیتابیس پیشنهادی
الگوی اصلی: Shared Database with Discriminator Pattern
┌─────────────────────────────────────────┐
│         Main PostgreSQL Database         │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────────────────────────────┐  │
│  │      Core Tables (Common)         │  │
│  ├──────────────────────────────────┤  │
│  │ - users                          │  │
│  │ - sites (site_type: enum)        │  │
│  │ - subscriptions                  │  │
│  │ - themes                         │  │
│  │ - pages (dynamic)                │  │
│  │ - media_library                  │  │
│  └──────────────────────────────────┘  │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │   Feature-Specific Tables         │  │
│  ├──────────────────────────────────┤  │
│  │ - blog_posts (site_id FK)        │  │
│  │ - blog_categories                │  │
│  │ - restaurant_menu_items          │  │
│  │ - restaurant_categories          │  │
│  │ - ecommerce_products             │  │
│  │ - ecommerce_orders               │  │
│  │ - portfolio_projects             │  │
│  └──────────────────────────────────┘  │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │   Polymorphic Content System      │  │
│  ├──────────────────────────────────┤  │
│  │ - content_blocks (JSON)          │  │
│  │ - page_sections (JSON)           │  │
│  │ - custom_fields (EAV)            │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘

📊 Schema کلیدی دیتابیس Django
1. Core Models
python# core/models.py

class SiteType(models.TextChoices):
    RESTAURANT = 'restaurant', 'Restaurant'
    BLOG = 'blog', 'Blog'
    ECOMMERCE = 'ecommerce', 'E-commerce'
    PORTFOLIO = 'portfolio', 'Portfolio'
    LANDING = 'landing', 'Landing Page'
    CORPORATE = 'corporate', 'Corporate'

class Site(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE)
    site_type = models.CharField(max_length=20, choices=SiteType.choices)
    subdomain = models.CharField(max_length=100, unique=True)
    custom_domain = models.CharField(max_length=200, null=True, blank=True)
    theme = models.ForeignKey('Theme', on_delete=models.SET_NULL, null=True)
    is_active = models.BooleanField(default=True)
    settings = models.JSONField(default=dict)  # Site-specific settings
    created_at = models.DateTimeField(auto_now_add=True)
    
class Theme(models.Model):
    name = models.CharField(max_length=100)
    site_types = models.JSONField(default=list)  # ['restaurant', 'blog']
    config = models.JSONField(default=dict)  # Colors, fonts, layouts
    preview_url = models.URLField()

class Page(models.Model):
    site = models.ForeignKey(Site, on_delete=models.CASCADE)
    title = models.CharField(max_length=200)
    slug = models.SlugField()
    is_homepage = models.BooleanField(default=False)
    sections = models.JSONField(default=list)  # Dynamic page builder data
    seo_meta = models.JSONField(default=dict)
2. Feature-Specific Models (Conditional)
python# blog/models.py
class BlogPost(models.Model):
    site = models.ForeignKey('core.Site', on_delete=models.CASCADE, 
                             limit_choices_to={'site_type': 'blog'})
    title = models.CharField(max_length=300)
    slug = models.SlugField()
    content = models.JSONField()  # Editor.js or similar format
    author = models.ForeignKey(User, on_delete=models.CASCADE)
    category = models.ForeignKey('BlogCategory', on_delete=models.SET_NULL, null=True)
    published_at = models.DateTimeField(null=True, blank=True)
    
# restaurant/models.py
class MenuItem(models.Model):
    site = models.ForeignKey('core.Site', on_delete=models.CASCADE,
                             limit_choices_to={'site_type': 'restaurant'})
    name = models.CharField(max_length=200)
    description = models.TextField()
    price = models.DecimalField(max_digits=10, decimal_places=2)
    category = models.ForeignKey('MenuCategory', on_delete=models.SET_NULL, null=True)
    image = models.ImageField(upload_to='menu/')
    is_available = models.BooleanField(default=True)

# ecommerce/models.py
class Product(models.Model):
    site = models.ForeignKey('core.Site', on_delete=models.CASCADE,
                             limit_choices_to={'site_type': 'ecommerce'})
    name = models.CharField(max_length=200)
    sku = models.CharField(max_length=50, unique=True)
    price = models.DecimalField(max_digits=10, decimal_places=2)
    stock = models.IntegerField(default=0)
    variants = models.JSONField(default=list)  # sizes, colors, etc.
3. Dynamic Content System (برای Page Builder)
python# builder/models.py
class ContentBlock(models.Model):
    """برای ذخیره بلوک‌های قابل استفاده مجدد"""
    site = models.ForeignKey(Site, on_delete=models.CASCADE)
    block_type = models.CharField(max_length=50)  # 'hero', 'features', 'cta', etc.
    data = models.JSONField()  # کامل داده‌های بلوک
    name = models.CharField(max_length=100)

class CustomField(models.Model):
    """EAV Pattern برای فیلدهای سفارشی"""
    site = models.ForeignKey(Site, on_delete=models.CASCADE)
    entity_type = models.CharField(max_length=50)  # 'product', 'post', 'page'
    entity_id = models.IntegerField()
    field_key = models.CharField(max_length=100)
    field_value = models.JSONField()
```

---

## 🏗️ ساختار Backend (Django Apps)
```
backend/
├── config/              # Django settings
├── core/                # Core models (User, Site, Theme, Subscription)
├── api/                 # API endpoints (DRF)
│   ├── serializers/
│   ├── views/
│   └── permissions.py
├── blog/                # Blog-specific app
│   ├── models.py
│   ├── admin.py
│   └── api.py
├── restaurant/          # Restaurant-specific app
├── ecommerce/           # E-commerce-specific app
├── portfolio/           # Portfolio-specific app
├── builder/             # Page builder core
│   ├── models.py (ContentBlock, Section)
│   ├── serializers.py
│   └── editor/          # Editor logic
├── media_library/       # Centralized media management
└── analytics/           # Analytics for all site types

🔌 API Architecture
RESTful Structure با Django REST Framework
python# api/urls.py
urlpatterns = [
    # Core
    path('sites/', SiteViewSet.as_view({'get': 'list', 'post': 'create'})),
    path('sites/<uuid:site_id>/', SiteDetailView.as_view()),
    
    # Dynamic routing based on site_type
    path('sites/<uuid:site_id>/content/', include('api.content_router')),
    
    # Feature-specific endpoints
    path('sites/<uuid:site_id>/blog/', include('blog.api.urls')),
    path('sites/<uuid:site_id>/menu/', include('restaurant.api.urls')),
    path('sites/<uuid:site_id>/products/', include('ecommerce.api.urls')),
]

# api/content_router.py (Dynamic router)
class ContentRouter:
    def get_urls(self, site):
        if site.site_type == SiteType.BLOG:
            return blog_urls
        elif site.site_type == SiteType.RESTAURANT:
            return restaurant_urls
        # ...

🎨 Frontend Dashboard: Single Adaptive Dashboard
رویکرد: Component-Based با Feature Flags
typescript// types/site.ts
export type SiteType = 'restaurant' | 'blog' | 'ecommerce' | 'portfolio';

export interface Site {
  id: string;
  type: SiteType;
  features: {
    blog?: boolean;
    ecommerce?: boolean;
    menu?: boolean;
    booking?: boolean;
  };
}

// components/dashboard/DashboardLayout.tsx
export default function DashboardLayout({ site }: { site: Site }) {
  const availableModules = getModulesForSite(site);
  
  return (
    <div>
      <Sidebar modules={availableModules} />
      <MainContent>
        {children}
      </MainContent>
    </div>
  );
}

// lib/dashboard-config.ts
export function getModulesForSite(site: Site) {
  const baseModules = ['analytics', 'pages', 'media', 'settings'];
  
  const typeSpecificModules = {
    blog: ['posts', 'categories', 'comments'],
    restaurant: ['menu', 'categories', 'qr-code'],
    ecommerce: ['products', 'orders', 'inventory'],
    portfolio: ['projects', 'testimonials'],
  };
  
  return [...baseModules, ...typeSpecificModules[site.type]];
}

⚡ جواب سوالات شما:
❌ نباید انجام دهید:

❌ بک‌اند جداگانه برای هر نوع سایت
❌ داشبورد جداگانه برای هر نوع
❌ DuckDB یا دیتابیس جداگانه برای هر سایت

✅ باید انجام دهید:

✅ یک Django Backend با app های ماژولار
✅ یک PostgreSQL مرکزی با Multi-Tenant pattern
✅ یک داشبورد Next.js با قابلیت Dynamic Module Loading
✅ Feature Flag System برای فعال/غیرفعال کردن ویژگی‌ها
✅ JSON Fields برای ذخیره تنظیمات منعطف


🚀 مزایای این معماری:

Scalability: افزودن نوع سایت جدید فقط نیاز به یک Django app دارد
Maintainability: یک codebase مرکزی برای نگهداری
Cost-Effective: یک سرور، یک دیتابیس
Flexibility: ترکیب ویژگی‌ها (مثلاً رستوران + بلاگ)
Shared Resources: کاربران، احراز هویت، media library مشترک


📝 Next Steps برای پیاده‌سازی:

Setup Django با PostgreSQL
ایجاد Core models (User, Site, Theme)
پیاده‌سازی اولین Django app (مثلاً blog)
ساخت API endpoints با DRF
اتصال Next.js به API
پیاده‌سازی Dynamic Dashboard
افزودن Page Builder با JSON-based storage

این معماری به شما اجازه می‌دهد با یک پایه قوی شروع کنید و به تدریج ویژگی‌های جدید اضافه کنید. 🎯بدون کد نویسی بهم توضیح بده چه معماری پیاده کنیم چه روشی و چی بهتره بارم توضیح بده اصلابگو بک اند جنگو براش مناسبه؟2:47 PMتوضیح کامل معماری بدون کد - رویکرد درست برای Site Builder
بذار خیلی ساده و بدون کد برات توضیح بدم که چطور باید این پلتفرم رو بسازی.

🤔 اول بفهمیم مشکل اصلی چیه؟
شما می‌خواید یک پلتفرم بسازید که:

هم رستوران بسازه
هم بلاگ بسازه
هم فروشگاه بسازه
هم سایت شرکتی بسازه

سوال اصلی: هر کدوم از اینها نیازمندی‌های متفاوتی دارن:

بلاگ نیاز به ادیتور متن و پست داره
رستوران نیاز به منو و کد QR داره
فروشگاه نیاز به سبد خرید و پرداخت داره

پس چطوری همه اینارو در یک سیستم مدیریت کنیم؟

🎯 راه‌حل‌های ممکن (و چرا بعضی‌ها بد هستن)
❌ راه‌حل ۱: بک‌اند جداگانه برای هر نوع سایت
یعنی یک بک‌اند برای بلاگ، یک بک‌اند برای رستوران، ...
چرا بد است؟

باید ۵-۶ تا بک‌اند نگهداری کنی
اگر کاربر بخواد رستوران + بلاگ داشته باشه چی؟
کدت تکراری میشه
خیلی گرون و پیچیده میشه

❌ راه‌حل ۲: یک دیتابیس کوچک (DuckDB) برای هر سایت
یعنی هر سایتی که کاربر میسازه، یک فایل دیتابیس جداگانه داشته باشه.
چرا بد است؟

نمیتونی روی همه سایت‌ها گزارش بگیری
مدیریت هزاران فایل دیتابیس کابوسه
Backup و بازیابی خیلی سخته
نمیتونی منابع رو share کنی (مثلاً یک عکس در چند سایت)

✅ راه‌حل ۳: یک بک‌اند با معماری ماژولار (درست)
یک بک‌اند که همه چیز رو مدیریت میکنه ولی به صورت ماژول‌های جداگانه.

🏗️ معماری پیشنهادی (ساده شده)
تصور کن یک ساختمان بزرگ داری:
طبقه اول (پایه): Core
این طبقه برای همه انواع سایت‌ها مشترکه:

مدیریت کاربران (ثبت نام، لاگین)
مدیریت سایت‌ها (هر کاربر چند تا سایت میتونه داشته باشه)
مدیریت اشتراک (پرداخت، پلن‌ها)
مدیریت تم‌ها (قالب‌های آماده)
کتابخانه رسانه (جایی که همه عکس‌ها ذخیره میشن)

طبقه دوم: اتاق‌های تخصصی (Modules)
هر اتاق مخصوص یک نوع سایته:
اتاق ۱ - ماژول بلاگ:

جدول پست‌ها
جدول دسته‌بندی‌ها
ادیتور متن
سیستم کامنت

اتاق ۲ - ماژول رستوران:

جدول منو
جدول دسته‌بندی غذاها
ساخت QR کد

اتاق ۳ - ماژول فروشگاه:

جدول محصولات
جدول سفارشات
سیستم پرداخت
موجودی انبار

اتاق ۴ - ماژول پرتفولیو:

جدول پروژه‌ها
گالری تصاویر

طبقه سوم: سیستم صفحه‌ساز (Page Builder)
این بخش به کاربر اجازه میده صفحات سفارشی بسازه:

بلوک‌های آماده (Hero, Features, Gallery)
ذخیره داده به صورت JSON
Drag & Drop برای چیدمان


💾 چطور دیتابیس طراحی کنیم؟
استراتژی: یک دیتابیس مرکزی با جداول مشترک و اختصاصی
تصور کن یک کتابخانه بزرگ:
قفسه‌های مشترک (جداول Core):
همه سایت‌ها از اینا استفاده می‌کنن:

جدول کاربران
جدول سایت‌ها (هر سایت یک رکورد)
جدول صفحات
جدول فایل‌های آپلود شده

قفسه‌های تخصصی (جداول Feature-Specific):
فقط سایت‌هایی که نیاز دارن ازشون استفاده می‌کنن:

جدول پست‌های بلاگ (فقط برای سایت‌های بلاگ)
جدول منوی رستوران (فقط برای رستوران‌ها)
جدول محصولات (فقط برای فروشگاه‌ها)

چطوری به هم وصل میشن؟
هر جدول اختصاصی یک ستون داره به اسم site_id که میگه "این رکورد متعلق به کدوم سایته".
مثال:
سایت شماره ۱: رستوران ایرانی
- در جدول sites: نوع = رستوران
- در جدول menu_items: ۲۰ تا غذا با site_id = 1

سایت شماره ۲: بلاگ شخصی
- در جدول sites: نوع = بلاگ  
- در جدول blog_posts: ۵۰ تا پست با site_id = 2

🎨 یک داشبورد یا چند داشبورد؟
✅ راه‌حل: یک داشبورد هوشمند
داشبورد بسته به نوع سایتی که کاربر داره، منو و قسمت‌هاش رو عوض می‌کنه.
مثال:
کاربر با سایت رستوران میبینه:

آمار بازدیدها ✓
مدیریت صفحات ✓
مدیریت منو ✓ (اختصاصی)
ساخت QR کد ✓ (اختصاصی)
تنظیمات ✓

کاربر با سایت بلاگ میبینه:

آمار بازدیدها ✓
مدیریت صفحات ✓
مدیریت پست‌ها ✓ (اختصاصی)
دسته‌بندی‌ها ✓ (اختصاصی)
کامنت‌ها ✓ (اختصاصی)
تنظیمات ✓

چطوری این کار رو میکنیم؟
فرانت‌اند Next.js وقتی میفهمه سایت از نوع "رستوران" هست، فقط کامپوننت‌های مربوط به رستوران رو نمایش میده.

🔧 جنگو مناسب هست یا نه؟
✅ بله، جنگو عالیه - اما با شرط‌هایی:
مزایای جنگو برای این پروژه:

Django Apps (ماژولار بودن)

می‌تونی هر ویژگی رو توی یک App جدا بنویسی
خیلی راحت App جدید اضافه می‌کنی
کد تمیز و سازمان‌یافته میشه


ORM قدرتمند

کار با دیتابیس خیلی راحته
میتونی روابط پیچیده بسازی
Query optimization خودکاره


Django Admin

یک پنل ادمین رایگان داری
برای مدیریت کل سیستم عالیه


Django REST Framework

ساخت API خیلی سریعه
Authentication و Permission آماده داره
مستندات خودکار


Ecosystem غنی

کلی پکیج آمادست (پرداخت، ایمیل، فایل‌ها)
Community بزرگ و فعال



محدودیت‌های جنگو:

کمی سنگینه

برای سایت‌های با ترافیک خیلی بالا باید optimization بیشتری بکنی


Real-time ضعیف‌تره

اگه نیاز به WebSocket و real-time داری، باید از Django Channels استفاده کنی



آیا گزینه‌های بهتری هست؟
FastAPI:

سریع‌تره
مدرن‌تره
ولی باید خودت همه چیز رو بسازی (Authentication, Admin, ...)

Node.js (NestJS):

خوب برای real-time
اگه تیمت JavaScript بلده راحت‌تره
ولی ORM‌هاش به قدرت Django نیستن

نتیجه: اگه تیمت Python بلده، Django بهترین گزینه است چون:

سرعت توسعه بالاست
همه چیز out-of-the-box داره
برای MVP و رشد سریع عالیه


📊 خلاصه معماری نهایی
┌─────────────────────────────────────────┐
│          Frontend (Next.js)              │
│  - یک داشبورد هوشمند                     │
│  - نمایش ماژول‌ها بر اساس نوع سایت      │
└─────────────────────────────────────────┘
                    ↓ API Calls
┌─────────────────────────────────────────┐
│       Backend (Django - یک سرور)         │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │   Core App (مشترک)                │  │
│  │  - Users, Sites, Themes           │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌─────────┬─────────┬──────────────┐  │
│  │Blog App │Rest. App│Ecommerce App │  │
│  │(ماژول)  │(ماژول)  │(ماژول)       │  │
│  └─────────┴─────────┴──────────────┘  │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│    PostgreSQL (یک دیتابیس مرکزی)        │
│  - جداول مشترک                          │
│  - جداول اختصاصی هر ماژول               │
└─────────────────────────────────────────┘

✅ گام‌های شروع پروژه (به ترتیب اولویت)

Setup اولیه:

Django + PostgreSQL
ساختار App های Core


پیاده‌سازی اولین ماژول:

شروع با ساده‌ترین (مثلاً رستوران)
تست کامل


ساخت API:

Django REST Framework
Authentication


اتصال فرانت‌اند:

Next.js به API وصل شه
داشبورد اولیه


افزودن ماژول‌های بعدی:

Blog
E-commerce
...


Page Builder:

سیستم Drag & Drop
ذخیره در JSON



این معماری بهت اجازه میده تدریجی پیشرفت کنی و بدون اینکه کل سیستم رو دوباره بنویسی، ویژگی جدید اضافه کنی. 🎯بیا یه راهنما برام بنویس که هرچیزی رو با چه معماری پیاده کنم و اینکه در نهایت fast api یا django یا node کدوم برای بک اند این پروژه مناسب تره2:51 PM🗺️ راهنمای کامل معماری و انتخاب تکنولوژی برای Site Builder Platform

📋 فهرست مطالب

تحلیل نیازمندی‌های پروژه
مقایسه تکنولوژی‌های Backend
انتخاب نهایی و دلایل
معماری کامل سیستم
نقشه راه پیاده‌سازی


🔍 بخش ۱: تحلیل نیازمندی‌های پروژه {#analysis}
چه چیزهایی نیاز داریم؟
۱. قابلیت‌های اصلی (Core Features)

✅ مدیریت کاربران (ثبت‌نام، لاگین، پروفایل)
✅ سیستم Multi-tenant (هر کاربر چند سایت)
✅ مدیریت Subscription (پلن‌های اشتراکی، پرداخت)
✅ سیستم Theme/Template
✅ Media Library مرکزی
✅ Analytics و Reporting

۲. ماژول‌های اختصاصی

📝 Blog Module: ادیتور، پست، دسته‌بندی، کامنت، SEO
🍽️ Restaurant Module: منو، دسته‌بندی غذا، QR کد، ساعات کاری
🛒 E-commerce Module: محصولات، سبد خرید، پرداخت، موجودی، سفارشات
💼 Portfolio Module: پروژه‌ها، کیس استادی، تیم
🏢 Corporate Module: خدمات، تیم، فرم تماس

۳. سیستم Page Builder

Drag & Drop Interface
بلوک‌های از پیش ساخته
ذخیره‌سازی پویا (JSON-based)
سیستم Responsive
SEO Management

۴. نیازمندی‌های فنی

مقیاس‌پذیری: احتمال رشد سریع کاربران
عملکرد: سرعت بارگذاری صفحات
امنیت: محافظت از داده‌های کاربران
قابلیت توسعه: افزودن ماژول‌های جدید راحت باشه
API-First: فرانت‌اند و بک‌اند کاملاً جدا
File Handling: آپلود و مدیریت تصاویر و فایل‌ها
Background Jobs: ارسال ایمیل، پردازش تصاویر، گزارش‌گیری


⚖️ بخش ۲: مقایسه عمیق تکنولوژی‌ها {#comparison}
🐍 Django
✅ مزایا:

Batteries Included Philosophy

ORM قدرتمند و mature
Django Admin (پنل مدیریت رایگان)
Authentication/Authorization کامل
Security built-in (CSRF, XSS, SQL Injection)


برای پروژه Multi-tenant عالیه

مدیریت دیتابیس پیچیده راحته
Django Apps برای ماژولار کردن
Migration system قوی


Ecosystem غنی

Django REST Framework (DRF)
Celery برای Background Jobs
Django Channels برای WebSocket
پکیج‌های آماده برای پرداخت، ایمیل، Storage


مناسب برای تیم‌های کوچک

سرعت توسعه بالا
کمتر نیاز به تصمیم‌گیری معماری


مستندات عالی و Community بزرگ

❌ معایب:

عملکرد کمی پایین‌تر

نسبت به FastAPI و Node.js کندتره
برای Async کار اضافه نیاز داره


سنگین بودن

حتی برای API ساده، کل فریمورک لود میشه


Monolithic تمایل داره

اگه دقت نکنی، کد درهم میره



🎯 امتیاز کلی: 8.5/10
مناسب برای: پروژه‌های CRUD-heavy، نیاز به Admin Panel، تیم‌های Python

⚡ FastAPI
✅ مزایا:

سرعت بالا

یکی از سریع‌ترین فریمورک‌های Python
Async/Await native support
Performance نزدیک به Node.js


Modern و Developer-Friendly

Type hints (خطاها رو زودتر میگیری)
مستندات خودکار (Swagger/OpenAPI)
کد تمیزتر و خواناتر


سبک و منعطف

فقط چیزی که نیاز داری رو اضافه می‌کنی
Microservices friendly


برای API عالیه

طراحی شده فقط برای API
Validation خودکار با Pydantic



❌ معایب:

باید همه چیز رو خودت بسازی

Admin Panel نداره (باید بسازی یا از SQLAdmin استفاده کنی)
Authentication باید خودت پیاده کنی
Background Jobs باید جدا setup کنی


Ecosystem کوچک‌تر

نسبت به Django پکیج‌های آماده کمتره
باید بیشتر چیزها رو custom بنویسی


ORM ضعیف‌تر

SQLAlchemy خوبه ولی به قدرت Django ORM نیست
Migration کمی دستیه


سرعت توسعه پایین‌تر

برای MVP بیشتر وقت می‌بره



🎯 امتیاز کلی: 7.5/10
مناسب برای: API-only projects، Microservices، نیاز به Performance بالا

🟢 Node.js (NestJS)
✅ مزایا:

JavaScript/TypeScript در هر دو سمت

اگه تیمت فقط JS بلده، ایده‌آله
Code sharing بین Frontend و Backend


Real-time عالی

WebSocket native
برای چت، نوتیفیکیشن real-time بی‌نظیره


سرعت بالا

Non-blocking I/O
مناسب برای concurrent requests زیاد


NestJS خاص:

معماری شبیه Angular (خوب برای پروژه‌های بزرگ)
Dependency Injection
TypeScript first


NPM Ecosystem

بزرگترین Package Registry دنیا



❌ معایب:

ORM ضعیف

TypeORM و Prisma خوبن ولی به Django ORM نمی‌رسن
کار با روابط پیچیده سخت‌تره


نیاز به معماری دقیق

اگه درست طراحی نکنی، Callback Hell میشی
برای مبتدی سخت‌تره


Single-threaded

برای CPU-intensive tasks (مثل پردازش تصویر) ضعیفه


Security نیاز به دقت بیشتر

باید خودت همه چیز رو handle کنی


Admin Panel نداره

🎯 امتیاز کلی: 7/10
مناسب برای: Real-time apps، تیم‌های JavaScript، Microservices

🏆 بخش ۳: انتخاب نهایی {#final-choice}
برنده: Django 🎖️
چرا Django برای این پروژه بهترین گزینه است؟
۱. ماهیت پروژه شما CRUD-Heavy است

۸۰٪ کارت Create, Read, Update, Delete است
Django برای همین طراحی شده
FastAPI و Node برای API-heavy یا Real-time بهترن

۲. نیاز به Admin Panel داری

مدیریت کاربران، سایت‌ها، پرداخت‌ها
Django Admin رایگان و قدرتمند
با FastAPI/Node باید خودت بسازی (۲-۳ ماه کار اضافه)

۳. Multi-tenant Complexity

روابط پیچیده بین User → Sites → Content
Django ORM این کارها رو راحت می‌کنه
Migration management عالیه

۴. سرعت توسعه (Time to Market)

برای MVP سریع‌ترین گزینه
پکیج‌های آماده برای پرداخت، ایمیل، Storage
Authentication out-of-the-box

۵. تیم کوچک

احتمالاً تیمت کوچیکه (۱-۳ نفر)
Django بهت اجازه میده سریع‌تر پیشرفت کنی
کمتر نیاز به تصمیم‌گیری معماری

۶. مقیاس‌پذیری کافی

Instagram, Pinterest, Spotify از Django استفاده می‌کنن
اگه درست بنویسی، تا میلیون‌ها کاربر Scale میکنه
وقتی بزرگ شدی، می‌تونی بخش‌هایی رو جدا کنی

⚠️ کی FastAPI یا Node انتخاب کنیم؟
FastAPI را انتخاب کن اگر:

Real-time چت یا نوتیفیکیشن اولویت اوله
Performance خیلی مهمه (۱۰۰k+ request per second)
تیمت Python pro هستن و دوست دارن همه چیز رو کنترل کنن

Node.js را انتخاب کن اگر:

تیمت فقط JavaScript بلده
نیاز به WebSocket و Real-time زیاد داری
می‌خوای از Next.js Server Actions استفاده کنی


🏗️ بخش ۴: معماری کامل سیستم {#architecture}
Architecture Pattern: Modular Monolith
چرا نه Microservices؟

در ابتدا نیازی نیست
پیچیدگی بالا
هزینه DevOps
وقتی رشد کردی میشه جدا کرد

۱. ساختار کلی
┌─────────────────────────────────────────────────────┐
│                  کاربران (Users)                    │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│              Frontend (Next.js 15)                  │
│  ┌────────────────────────────────────────────┐    │
│  │  - Landing Pages                           │    │
│  │  - Authentication UI                       │    │
│  │  - Dashboard (Adaptive/Dynamic)            │    │
│  │  - Page Builder (Visual Editor)            │    │
│  │  - Site Preview                            │    │
│  └────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
                         ↓
                    REST API / GraphQL
                         ↓
┌─────────────────────────────────────────────────────┐
│              Backend (Django)                       │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │         Core Module (همیشه فعال)            │  │
│  ├──────────────────────────────────────────────┤  │
│  │  - User Management                          │  │
│  │  - Site Management                          │  │
│  │  - Subscription & Billing                   │  │
│  │  - Theme System                             │  │
│  │  - Media Library                            │  │
│  │  - Analytics Engine                         │  │
│  │  - Page Builder Core                        │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │      Feature Modules (اختیاری/ماژولار)      │  │
│  ├──────────────────────────────────────────────┤  │
│  │  📝 Blog Module                             │  │
│  │     - Posts, Categories, Tags               │  │
│  │     - Comments System                       │  │
│  │     - Rich Text Editor API                  │  │
│  │                                              │  │
│  │  🍽️ Restaurant Module                      │  │
│  │     - Menu Items, Categories                │  │
│  │     - QR Code Generator                     │  │
│  │     - Opening Hours                         │  │
│  │                                              │  │
│  │  🛒 E-commerce Module                       │  │
│  │     - Products, Variants                    │  │
│  │     - Orders, Cart                          │  │
│  │     - Payment Integration                   │  │
│  │     - Inventory Management                  │  │
│  │                                              │  │
│  │  💼 Portfolio Module                        │  │
│  │     - Projects, Case Studies                │  │
│  │     - Gallery                               │  │
│  │                                              │  │
│  │  🏢 Corporate Module                        │  │
│  │     - Services, Team Members                │  │
│  │     - Contact Forms                         │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│         Data Layer (PostgreSQL)                     │
│                                                     │
│  Core Tables + Feature-Specific Tables              │
│  + JSON Fields for Dynamic Content                  │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│              External Services                      │
│  - S3/CloudFlare R2 (File Storage)                 │
│  - Redis (Cache + Queue)                           │
│  - Celery (Background Jobs)                        │
│  - Email Service (SendGrid/Mailgun)                │
│  - Payment Gateway (Stripe/ZarinPal)               │
└─────────────────────────────────────────────────────┘

۲. معماری دیتابیس (Database Schema Strategy)
رویکرد: Shared Database با Tenant Isolation
جداول اصلی (Core Tables):
═══════════════════════════════════════

┌─────────────────┐
│  users          │
├─────────────────┤
│ id (PK)         │
│ email           │
│ password        │
│ created_at      │
└─────────────────┘
         │
         │ 1:N
         ↓
┌─────────────────┐
│  sites          │
├─────────────────┤
│ id (PK)         │
│ user_id (FK)    │
│ site_type ENUM  │  ← 'blog', 'restaurant', 'ecommerce'
│ subdomain       │
│ custom_domain   │
│ theme_id (FK)   │
│ settings JSON   │  ← تنظیمات اختصاصی هر سایت
│ is_active       │
└─────────────────┘
         │
         │ 1:N
         ↓
┌─────────────────┐
│  pages          │
├─────────────────┤
│ id (PK)         │
│ site_id (FK)    │
│ title           │
│ slug            │
│ sections JSON   │  ← داده‌های Page Builder
│ seo_meta JSON   │
│ is_published    │
└─────────────────┘


جداول اختصاصی (Feature-Specific):
═══════════════════════════════════════

┌──────────────────┐
│  blog_posts      │  ← فقط برای site_type='blog'
├──────────────────┤
│ id (PK)          │
│ site_id (FK)     │  ← کلید تفکیک
│ title            │
│ slug             │
│ content JSON     │
│ author_id (FK)   │
│ category_id (FK) │
│ status           │
│ published_at     │
└──────────────────┘

┌──────────────────┐
│  menu_items      │  ← فقط برای site_type='restaurant'
├──────────────────┤
│ id (PK)          │
│ site_id (FK)     │  ← کلید تفکیک
│ name             │
│ description      │
│ price            │
│ category_id (FK) │
│ image_url        │
│ is_available     │
└──────────────────┘

┌──────────────────┐
│  products        │  ← فقط برای site_type='ecommerce'
├──────────────────┤
│ id (PK)          │
│ site_id (FK)     │  ← کلید تفکیک
│ name             │
│ sku              │
│ price            │
│ stock            │
│ variants JSON    │
└──────────────────┘
نکات کلیدی:
۱. Tenant Isolation:

همه جداول اختصاصی site_id دارن
هر Query باید با site_id فیلتر بشه
جلوی دسترسی Cross-Site رو میگیره

۲. JSON Fields کجا استفاده بشه:

تنظیمات سایت (رنگ‌ها، فونت‌ها، لوگو)
داده‌های Page Builder (بلوک‌ها و Layout)
فیلدهای سفارشی کاربر
داده‌های Variant محصولات

۳. Foreign Keys:

همه روابط با CASCADE یا SET_NULL مشخص بشه
برای حذف سایت، همه داده‌هاش پاک میشه


۳. معماری API
رویکرد: RESTful API با Django REST Framework
API Structure:
════════════════════════════════════

/api/v1/
├── auth/
│   ├── POST   /register
│   ├── POST   /login
│   ├── POST   /logout
│   ├── POST   /refresh
│   └── GET    /me

├── sites/
│   ├── GET    /sites/              (لیست سایت‌های کاربر)
│   ├── POST   /sites/              (ساخت سایت جدید)
│   ├── GET    /sites/{id}/
│   ├── PUT    /sites/{id}/
│   ├── DELETE /sites/{id}/
│   └── GET    /sites/{id}/stats/   (آمار)

├── pages/
│   ├── GET    /sites/{site_id}/pages/
│   ├── POST   /sites/{site_id}/pages/
│   ├── PUT    /sites/{site_id}/pages/{id}/
│   └── DELETE /sites/{site_id}/pages/{id}/

├── media/
│   ├── POST   /sites/{site_id}/media/upload
│   ├── GET    /sites/{site_id}/media/
│   └── DELETE /sites/{site_id}/media/{id}/

├── themes/
│   ├── GET    /themes/             (لیست تم‌های موجود)
│   └── PUT    /sites/{site_id}/theme/

├── blog/                           ← ماژول بلاگ
│   ├── GET    /sites/{site_id}/posts/
│   ├── POST   /sites/{site_id}/posts/
│   └── ...

├── restaurant/                     ← ماژول رستوران
│   ├── GET    /sites/{site_id}/menu/
│   ├── POST   /sites/{site_id}/menu/items/
│   └── GET    /sites/{site_id}/qr-code/

└── ecommerce/                      ← ماژول فروشگاه
    ├── GET    /sites/{site_id}/products/
    ├── POST   /sites/{site_id}/orders/
    └── ...
Authentication Strategy:

JWT Tokens (Access + Refresh)
Access Token: 15 دقیقه
Refresh Token: 7 روز
HTTPOnly Cookies برای امنیت بیشتر

Authorization:
pythonPermission Hierarchy:
├── IsSiteOwner        → فقط مالک سایت
├── IsSuperAdmin       → ادمین کل سیستم
└── IsAuthenticated    → کاربر لاگین کرده
```

---

### **۴. معماری Frontend (Next.js)**

#### **رویکرد: Feature-Based Folder Structure**
```
frontend/
├── app/
│   ├── (public)/              ← صفحات عمومی
│   │   ├── page.tsx           (Landing)
│   │   ├── pricing/
│   │   └── templates/
│   │
│   ├── (auth)/                ← احراز هویت
│   │   ├── login/
│   │   ├── register/
│   │   └── verify/
│   │
│   ├── dashboard/             ← داشبورد اصلی
│   │   ├── layout.tsx         (Sidebar, Header)
│   │   ├── page.tsx           (Overview)
│   │   │
│   │   ├── sites/             ← مدیریت سایت‌ها
│   │   │   ├── page.tsx
│   │   │   ├── new/
│   │   │   └── [siteId]/
│   │   │       ├── analytics/
│   │   │       ├── pages/
│   │   │       ├── media/
│   │   │       ├── settings/
│   │   │       │
│   │   │       ├── blog/      ← ماژول بلاگ
│   │   │       │   ├── posts/
│   │   │       │   └── categories/
│   │   │       │
│   │   │       ├── menu/      ← ماژول رستوران
│   │   │       │   ├── items/
│   │   │       │   └── qr-code/
│   │   │       │
│   │   │       └── products/  ← ماژول فروشگاه
│   │   │           ├── list/
│   │   │           └── orders/
│   │
│   └── builder/               ← Page Builder
│       └── [siteId]/[pageId]/
│
├── components/
│   ├── ui/                    ← کامپوننت‌های پایه
│   ├── dashboard/             ← کامپوننت‌های داشبورد
│   │   ├── DashboardLayout/
│   │   ├── Sidebar/
│   │   └── ModuleRegistry/    ← سیستم ماژول‌ها
│   │
│   ├── builder/               ← کامپوننت‌های Page Builder
│   │   ├── Canvas/
│   │   ├── BlockLibrary/
│   │   └── Inspector/
│   │
│   └── modules/               ← کامپوننت‌های اختصاصی
│       ├── blog/
│       ├── restaurant/
│       └── ecommerce/
│
├── lib/
│   ├── api/                   ← API Client
│   │   ├── sites.ts
│   │   ├── blog.ts
│   │   └── ...
│   │
│   ├── hooks/                 ← Custom Hooks
│   │   ├── useSite.ts
│   │   ├── useModules.ts
│   │   └── ...
│   │
│   └── config/
│       └── modules.ts         ← Module Registry Config
│
└── types/
    ├── site.ts
    ├── modules.ts
    └── ...
سیستم ماژول‌های پویا (Dynamic Module System):
typescript// lib/config/modules.ts

export const MODULE_REGISTRY = {
  blog: {
    label: 'بلاگ',
    icon: 'FileText',
    routes: [
      { path: 'posts', label: 'پست‌ها' },
      { path: 'categories', label: 'دسته‌بندی‌ها' },
    ],
    requiredSiteType: ['blog'],
  },
  
  restaurant: {
    label: 'رستوران',
    icon: 'UtensilsCrossed',
    routes: [
      { path: 'menu', label: 'منو' },
      { path: 'qr-code', label: 'کد QR' },
    ],
    requiredSiteType: ['restaurant'],
  },
  
  ecommerce: {
    label: 'فروشگاه',
    icon: 'ShoppingCart',
    routes: [
      { path: 'products', label: 'محصولات' },
      { path: 'orders', label: 'سفارشات' },
    ],
    requiredSiteType: ['ecommerce'],
  },
};

// کامپوننت Sidebar بر اساس نوع سایت، ماژول‌ها رو نمایش میده
```

---

### **۵. Page Builder Architecture**

#### **رویکرد: JSON-Based Block System**
```
Block Structure:
════════════════════════

{
  "blocks": [
    {
      "id": "hero-1",
      "type": "hero",
      "data": {
        "title": "عنوان",
        "subtitle": "توضیح",
        "backgroundImage": "/media/hero.jpg",
        "ctaText": "شروع کنید",
        "ctaLink": "/contact"
      },
      "style": {
        "backgroundColor": "#000",
        "textColor": "#fff"
      }
    },
    {
      "id": "features-1",
      "type": "features",
      "data": {
        "items": [...]
      }
    }
  ]
}
```

**کتابخانه بلوک‌ها:**
- Hero (انواع مختلف)
- Features Grid
- Image Gallery
- Testimonials
- Pricing Tables
- Contact Forms
- Blog Post List
- Menu Display
- Product Grid
- Custom HTML/Code

---

### **۶. File Storage Strategy**
```
Development:
├── Local Storage (media/ folder)

Production:
├── S3-Compatible Storage (CloudFlare R2 / AWS S3)
├── CDN (CloudFlare)
└── Image Optimization (Automatic)
```

**Organization:**
```
bucket/
├── {tenant_id}/
│   ├── media/
│   │   ├── {year}/{month}/
│   │   │   └── image.jpg
│   │   └── thumbnails/
│   └── themes/
│       └── custom-assets/

۷. Background Jobs Architecture
Celery + Redis:
pythonCommon Tasks:
═════════════════════════════

├── Email Tasks
│   ├── send_welcome_email
│   ├── send_invoice
│   └── send_notification

├── Media Processing
│   ├── generate_thumbnails
│   ├── optimize_images
│   └── process_video

├── Analytics
│   ├── calculate_daily_stats
│   └── generate_reports

├── Subscription
│   ├── check_expiring_subscriptions
│   └── process_payment

└── Maintenance
    ├── cleanup_old_files
    └── backup_database
```

---

## 🛣️ بخش ۵: نقشه راه پیاده‌سازی (Roadmap) {#roadmap}

### **Phase 1: Foundation (هفته ۱-۲)**

#### Backend:
- [x] Setup Django Project
- [x] Setup PostgreSQL
- [x] Core Models (User, Site, Theme)
- [x] Authentication (JWT)
- [x] Basic API endpoints

#### Frontend:
- [x] Setup Next.js
- [x] Authentication UI
- [x] Dashboard Layout
- [x] Basic Routing

---

### **Phase 2: اولین ماژول - Restaurant (هفته ۳-۴)**

#### Backend:
- [ ] Restaurant App
- [ ] Menu Models (MenuItem, Category)
- [ ] API endpoints
- [ ] QR Code Generator

#### Frontend:
- [ ] Menu Management UI
- [ ] QR Code Display
- [ ] Restaurant Template

**چرا از Restaurant شروع کنیم؟**
- ساده‌ترین ماژول
- CRUD پایه
- تست معماری

---

### **Phase 3: Page Builder Core (هفته ۵-۶)**

#### Backend:
- [ ] Page Model با JSON field
- [ ] Block Schema Validation
- [ ] Page API

#### Frontend:
- [ ] Drag & Drop Interface
- [ ] Block Library (۵-۶ بلوک اولیه)
- [ ] Live Preview
- [ ] Save/Publish System

---

### **Phase 4: Blog Module (هفته ۷-۸)**

#### Backend:
- [ ] Blog App
- [ ] Post, Category Models
- [ ] Rich Text Storage
- [ ] SEO Fields

#### Frontend:
- [ ] Post Editor (Tiptap/Editor.js)
- [ ] Post List/Grid
- [ ] Category Management
- [ ] Blog Template

---

### **Phase 5: Media Library (هفته ۹)**

- [ ] Upload System
- [ ] S3 Integration
- [ ] Image Optimization
- [ ] Media Browser UI

---

### **Phase 6: E-commerce Module (هفته ۱۰-۱۲)**

#### Backend:
- [ ] Product Models
- [ ] Order System
- [ ] Payment Integration (Stripe)
- [ ] Inventory Management

#### Frontend:
- [ ] Product Management
- [ ] Order Dashboard
- [ ] Store Template

---

### **Phase 7: Analytics & Reporting (هفته ۱۳)**

- [ ] Traffic Tracking
- [ ] Dashboard Charts
- [ ] Export Reports

---

### **Phase 8: Subscription & Billing (هفته ۱۴-۱۵)**

- [ ] Plan Management
- [ ] Payment Gateway
- [ ] Invoice System
- [ ] Usage Limits

---

### **Phase 9: Polish & Launch (هفته ۱۶+)**

- [ ] Testing (Unit, Integration)
- [ ] Performance Optimization
- [ ] SEO
- [ ] Documentation
- [ ] Marketing Site
- [ ] Beta Launch

---

## ✅ خلاصه تصمیمات نهایی
```
Backend:        Django + Django REST Framework
Database:       PostgreSQL (Single Database)
Cache/Queue:    Redis + Celery
File Storage:   CloudFlare R2 / S3
Frontend:       Next.js 15 (App Router)
API Pattern:    RESTful
Architecture:   Modular Monolith
Deployment:     Docker + DigitalOcean/AWS
چرا این Stack؟
✅ سرعت توسعه بالا
✅ مقیاس‌پذیری کافی برای ۱۰۰k+ کاربر
✅ Ecosystem غنی
✅ هزینه منطقی
✅ قابل نگهداری
✅ تیم کوچک می‌تونه مدیریت کنه
این معماری بهت اجازه میده سریع شروع کنی، بدون اضافه‌بار پیشرفت کنی، و وقتی نیاز شد Scale کنی. 🚀در نهایت الان به من یه توضیح به زبان ساده بنویس که برای هم بک اند هم دیتاببیس و هم فرانت اند چه معماری مناسب تره و اون معماری رو به زبان ساده توضیح بده و اون ساختار رو2:55 PM🎯 راهنمای نهایی: معماری ساده و کامل پروژه
بذار خیلی ساده و قدم به قدم توضیح بدم که چطور باید این پروژه رو بسازی.

🏢 تصور کن یک ساختمان ۳ طبقه داری
┌─────────────────────────────────────┐
│   طبقه ۳: فرانت‌اند (Next.js)       │  ← چیزی که کاربر میبینه
├─────────────────────────────────────┤
│   طبقه ۲: بک‌اند (Django)           │  ← مغز برنامه
├─────────────────────────────────────┤
│   طبقه ۱: دیتابیس (PostgreSQL)      │  ← انبار اطلاعات
└─────────────────────────────────────┘
حالا هر طبقه رو جداگانه توضیح میدم.

🗄️ طبقه ۱: دیتابیس (PostgreSQL)
چطور داده‌ها رو سازماندهی کنیم؟
تصور کن یک کتابخانه بزرگ داری با قفسه‌های مختلف:
قفسه‌های اصلی (برای همه استفاده میشه):
📚 قفسه کاربران
هر کاربر یک کارت عضویت داره:
- شناسه کاربر
- ایمیل
- رمز عبور
- تاریخ ثبت‌نام
📚 قفسه سایت‌ها
هر کاربر می‌تونه چند تا سایت داشته باشه:
- شناسه سایت
- متعلق به کدوم کاربر؟
- نوع سایت: رستوران؟ بلاگ؟ فروشگاه؟
- آدرس سایت (مثلاً: myrestaurant.site.com)
- تم انتخابی
- تنظیمات (رنگ‌ها، لوگو، ...)
- فعال / غیرفعال
📚 قفسه صفحات
هر سایت چند صفحه داره:
- شناسه صفحه
- متعلق به کدوم سایت؟
- عنوان صفحه
- محتوای صفحه (بلوک‌ها و عناصر)
- منتشر شده / پیش‌نویس
📚 قفسه فایل‌ها
همه عکس‌ها و فایل‌ها:
- شناسه فایل
- متعلق به کدوم سایت؟
- آدرس فایل
- نوع فایل
- اندازه

قفسه‌های تخصصی (فقط برای سایت‌های خاص):
📝 قفسه پست‌های بلاگ (فقط برای بلاگ‌ها)
- شناسه پست
- متعلق به کدوم سایت؟ ← این خیلی مهمه!
- عنوان
- متن پست
- نویسنده
- دسته‌بندی
- تاریخ انتشار
🍽️ قفسه منوی رستوران (فقط برای رستوران‌ها)
- شناسه غذا
- متعلق به کدوم سایت؟ ← این خیلی مهمه!
- نام غذا
- توضیحات
- قیمت
- عکس
- موجود / ناموجود
🛒 قفسه محصولات فروشگاه (فقط برای فروشگاه‌ها)
- شناسه محصول
- متعلق به کدوم سایت؟ ← این خیلی مهمه!
- نام محصول
- قیمت
- موجودی
- تصاویر

🔑 نکته کلیدی دیتابیس:
همه چیز در یک دیتابیس، ولی جدا از هم!
مثال واقعی:
سایت شماره ۱ (رستوران علی):
├── در جدول sites: نوع = رستوران
├── در جدول menu_items: 
│   ├── کباب کوبیده (site_id = 1)
│   ├── جوجه کباب (site_id = 1)
│   └── چلوکباب (site_id = 1)
└── در جدول blog_posts: هیچی! (چون رستورانه نه بلاگ)

سایت شماره ۲ (بلاگ حسین):
├── در جدول sites: نوع = بلاگ
├── در جدول blog_posts:
│   ├── مقاله درباره React (site_id = 2)
│   ├── مقاله درباره Django (site_id = 2)
│   └── نکات Next.js (site_id = 2)
└── در جدول menu_items: هیچی! (چون بلاگه نه رستوران)
چطوری اطمینان پیدا میکنیم داده‌ها قاطی نشن؟

هر بار که داده میخونیم، می‌گیم: "فقط بده داده‌های سایت شماره X"
هر بار که داده می‌نویسیم، می‌گیم: "این داده متعلق به سایت شماره X است"


⚙️ طبقه ۲: بک‌اند (Django)
Django رو به عنوان یک شرکت با بخش‌های مختلف تصور کن:
شرکت Django
├── دفتر مرکزی (Core)
│   ├── مدیریت کاربران
│   ├── مدیریت سایت‌ها
│   ├── مدیریت اشتراک‌ها
│   ├── مدیریت فایل‌ها
│   └── مدیریت تم‌ها
│
├── دفتر بلاگ (Blog Module)
│   ├── مدیریت پست‌ها
│   ├── مدیریت دسته‌بندی‌ها
│   └── مدیریت کامنت‌ها
│
├── دفتر رستوران (Restaurant Module)
│   ├── مدیریت منو
│   ├── ساخت QR کد
│   └── مدیریت ساعات کاری
│
└── دفتر فروشگاه (Ecommerce Module)
    ├── مدیریت محصولات
    ├── مدیریت سفارشات
    └── سیستم پرداخت
چطور کار می‌کنه؟
مثال ۱: کاربر می‌خواد پست جدید بسازه
گام ۱: کاربر از فرانت‌اند درخواست میده:
"یک پست جدید با عنوان 'آموزش React' برای سایت شماره ۲ بساز"

گام ۲: درخواست به بک‌اند میرسه (API Endpoint)
مسیر: POST /api/sites/2/blog/posts/

گام ۳: بک‌اند چک می‌کنه:
✓ آیا کاربر لاگین کرده?
✓ آیا سایت شماره ۲ متعلق به این کاربره؟
✓ آیا سایت شماره ۲ از نوع بلاگ هست؟

گام ۴: اگه همه چیز OK بود:
- یک رکورد جدید در جدول blog_posts میسازه
- با site_id = 2 ذخیره می‌کنه
- جواب میده: "پست با موفقیت ساخته شد"

گام ۵: فرانت‌اند جواب رو دریافت می‌کنه و به کاربر نشون میده
مثال ۲: کاربر می‌خواد لیست غذاهاش رو ببینه
گام ۱: کاربر میره تو داشبورد بخش منو
فرانت‌اند درخواست میده: GET /api/sites/1/menu/items/

گام ۲: بک‌اند:
- چک می‌کنه سایت ۱ متعلق به این کاربره
- از دیتابیس می‌پرسه: "همه menu_items که site_id = 1 دارن رو بده"
- لیست رو برمی‌گردونه

گام ۳: فرانت‌اند لیست رو دریافت می‌کنه و نمایش میده

📁 ساختار فایل‌های Django:
backend/
├── config/                    ← تنظیمات اصلی
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
│
├── core/                      ← بخش مرکزی (همه ازش استفاده می‌کنن)
│   ├── models.py
│   │   ├── User
│   │   ├── Site
│   │   ├── Theme
│   │   ├── Page
│   │   └── MediaFile
│   │
│   ├── views.py               ← منطق کار
│   ├── serializers.py         ← تبدیل داده برای API
│   └── urls.py                ← مسیرها
│
├── blog/                      ← ماژول بلاگ
│   ├── models.py
│   │   ├── BlogPost
│   │   ├── Category
│   │   └── Comment
│   │
│   ├── views.py
│   ├── serializers.py
│   └── urls.py
│
├── restaurant/                ← ماژول رستوران
│   ├── models.py
│   │   ├── MenuItem
│   │   ├── MenuCategory
│   │   └── OpeningHours
│   │
│   ├── views.py
│   ├── serializers.py
│   └── urls.py
│
└── ecommerce/                 ← ماژول فروشگاه
    ├── models.py
    │   ├── Product
    │   ├── Order
    │   └── Cart
    │
    ├── views.py
    ├── serializers.py
    └── urls.py

🎨 طبقه ۳: فرانت‌اند (Next.js)
فرانت‌اند رو مثل یک ساختمان اداری تصور کن:
ساختمان فرانت‌اند
├── طبقه همکف: صفحات عمومی
│   ├── صفحه اصلی (Landing)
│   ├── قیمت‌گذاری
│   ├── نمونه کارها
│   └── تماس با ما
│
├── طبقه اول: بخش ورود/ثبت‌نام
│   ├── لاگین
│   ├── ثبت‌نام
│   ├── فراموشی رمز
│   └── تایید حساب
│
└── طبقه دوم: داشبورد (فقط برای کاربران لاگین)
    ├── اتاق اصلی (Overview)
    ├── اتاق سایت‌ها
    │   └── برای هر سایت:
    │       ├── آمار
    │       ├── صفحات
    │       ├── فایل‌ها
    │       ├── تنظیمات
    │       │
    │       └── اتاق‌های اختصاصی (بسته به نوع سایت):
    │           ├── اگر بلاگ → اتاق پست‌ها
    │           ├── اگر رستوران → اتاق منو
    │           └── اگر فروشگاه → اتاق محصولات

🧩 داشبورد هوشمند چطور کار می‌کنه؟
تصور کن یک داشبورد داری که مثل یک پازل خودش رو تغییر میده:
وقتی کاربر لاگین می‌کنه:
گام ۱: از بک‌اند می‌پرسه: "سایت‌های من چی هستن?"
جواب: 
- سایت ۱: رستوران
- سایت ۲: بلاگ

گام ۲: کاربر روی سایت ۱ (رستوران) کلیک می‌کنه

گام ۳: فرانت‌اند می‌فهمه این سایت "رستوران" هست

گام ۴: منوی سمت چپ رو می‌سازه:
┌─────────────────┐
│ 📊 آمار         │ ← همیشه هست
│ 📄 صفحات        │ ← همیشه هست
│ 🖼️ فایل‌ها      │ ← همیشه هست
│ ─────────────── │
│ 🍽️ مدیریت منو  │ ← فقط برای رستوران
│ 📱 کد QR        │ ← فقط برای رستوران
│ ─────────────── │
│ ⚙️ تنظیمات      │ ← همیشه هست
└─────────────────┘

گام ۵: اگه روی سایت ۲ (بلاگ) کلیک کنه:
┌─────────────────┐
│ 📊 آمار         │ ← همیشه هست
│ 📄 صفحات        │ ← همیشه هست
│ 🖼️ فایل‌ها      │ ← همیشه هست
│ ─────────────── │
│ 📝 پست‌ها       │ ← فقط برای بلاگ
│ 📁 دسته‌بندی‌ها  │ ← فقط برای بلاگ
│ 💬 کامنت‌ها     │ ← فقط برای بلاگ
│ ─────────────── │
│ ⚙️ تنظیمات      │ ← همیشه هست
└─────────────────┘

📂 ساختار فایل‌های Next.js:
frontend/
├── app/
│   ├── page.tsx                        ← صفحه اصلی (لندینگ)
│   ├── pricing/page.tsx                ← قیمت‌گذاری
│   │
│   ├── login/page.tsx                  ← لاگین
│   ├── register/page.tsx               ← ثبت‌نام
│   │
│   └── dashboard/
│       ├── layout.tsx                  ← قالب داشبورد (Sidebar + Header)
│       ├── page.tsx                    ← صفحه اصلی داشبورد
│       │
│       └── sites/
│           └── [siteId]/               ← برای هر سایت
│               ├── page.tsx            ← نمای کلی سایت
│               ├── analytics/          ← آمار
│               ├── pages/              ← مدیریت صفحات
│               ├── media/              ← مدیریت فایل‌ها
│               │
│               ├── blog/               ← فقط اگه سایت بلاگ باشه
│               │   ├── posts/
│               │   └── categories/
│               │
│               ├── menu/               ← فقط اگه سایت رستوران باشه
│               │   ├── items/
│               │   └── qr-code/
│               │
│               └── products/           ← فقط اگه سایت فروشگاه باشه
│                   ├── list/
│                   └── orders/
│
├── components/
│   ├── ui/                             ← دکمه، اینپوت، کارت، ...
│   ├── dashboard/
│   │   ├── Sidebar.tsx                 ← منوی سمت چپ
│   │   ├── Header.tsx                  ← هدر بالا
│   │   └── ModuleLoader.tsx            ← سیستم بارگذاری ماژول‌ها
│   │
│   └── modules/                        ← کامپوننت‌های اختصاصی
│       ├── blog/
│       │   ├── PostEditor.tsx
│       │   └── PostList.tsx
│       │
│       ├── restaurant/
│       │   ├── MenuItemForm.tsx
│       │   └── QRCodeDisplay.tsx
│       │
│       └── ecommerce/
│           ├── ProductForm.tsx
│           └── OrderList.tsx
│
└── lib/
    ├── api.ts                          ← تابع‌های ارتباط با بک‌اند
    └── config/
        └── modules.ts                  ← تنظیمات ماژول‌ها

🔧 فایل تنظیمات ماژول‌ها (ساده‌شده):
typescript// lib/config/modules.ts

const MODULES = {
  // ماژول بلاگ
  blog: {
    name: 'بلاگ',
    icon: '📝',
    menuItems: [
      { label: 'پست‌ها', path: 'posts' },
      { label: 'دسته‌بندی‌ها', path: 'categories' },
    ],
    showFor: ['blog'],  // فقط برای سایت‌های نوع بلاگ
  },
  
  // ماژول رستوران
  restaurant: {
    name: 'رستوران',
    icon: '🍽️',
    menuItems: [
      { label: 'منو', path: 'menu' },
      { label: 'کد QR', path: 'qr-code' },
    ],
    showFor: ['restaurant'],  // فقط برای سایت‌های نوع رستوران
  },
  
  // ماژول فروشگاه
  ecommerce: {
    name: 'فروشگاه',
    icon: '🛒',
    menuItems: [
      { label: 'محصولات', path: 'products' },
      { label: 'سفارشات', path: 'orders' },
    ],
    showFor: ['ecommerce'],  // فقط برای سایت‌های نوع فروشگاه
  },
};
چطور استفاده میشه؟
typescript// کامپوننت Sidebar

function Sidebar({ currentSite }) {
  // منوهای همیشگی
  const baseMenuItems = ['آمار', 'صفحات', 'فایل‌ها'];
  
  // پیدا کردن ماژول‌های مناسب
  const availableModules = Object.values(MODULES).filter(
    module => module.showFor.includes(currentSite.type)
  );
  
  return (
    <div>
      {/* منوهای پایه */}
      {baseMenuItems.map(...)}
      
      {/* منوهای اختصاصی */}
      {availableModules.map(module => (
        <div>
          <h3>{module.icon} {module.name}</h3>
          {module.menuItems.map(...)}
        </div>
      ))}
    </div>
  );
}
```

---

## 🔄 جریان کامل کار (مثال واقعی)

### **سناریو: کاربر می‌خواد یک غذا به منو رستورانش اضافه کنه**
```
┌────────────────────────────────────────────────────────┐
│ گام ۱: کاربر در فرانت‌اند                              │
└────────────────────────────────────────────────────────┘
کاربر میره تو داشبورد → سایت رستوران → بخش منو → دکمه "افزودن غذا"
فرم رو پر می‌کنه:
- نام: کباب کوبیده
- قیمت: ۲۵۰,۰۰۰ تومان
- توضیحات: کباب کوبیده مخصوص با گوشت تازه
- عکس: آپلود می‌کنه
- دسته‌بندی: کباب‌ها

روی "ذخیره" کلیک می‌کنه

┌────────────────────────────────────────────────────────┐
│ گام ۲: ارسال به بک‌اند                                │
└────────────────────────────────────────────────────────┘
فرانت‌اند یک درخواست HTTP میفرسته:
POST /api/sites/123/menu/items/
Body: {
  name: "کباب کوبیده",
  price: 250000,
  description: "...",
  image: (فایل),
  category_id: 5
}
Headers: {
  Authorization: "Bearer [توکن کاربر]"
}

┌────────────────────────────────────────────────────────┐
│ گام ۳: بک‌اند چک می‌کنه                               │
└────────────────────────────────────────────────────────┘
✓ توکن معتبره؟
✓ کاربر لاگین کرده؟
✓ سایت ۱۲۳ متعلق به این کاربره؟
✓ سایت ۱۲۳ از نوع رستوران هست؟
✓ داده‌های ارسالی صحیح هستن؟

┌────────────────────────────────────────────────────────┐
│ گام ۴: ذخیره در دیتابیس                               │
└────────────────────────────────────────────────────────┘
اول عکس رو آپلود می‌کنه روی سرور:
/media/sites/123/menu/kabab-koobideh.jpg

بعد رکورد رو در جدول menu_items ذخیره می‌کنه:
- id: 456
- site_id: 123  ← خیلی مهم!
- name: "کباب کوبیده"
- price: 250000
- description: "..."
- image_url: "/media/sites/123/menu/kabab-koobideh.jpg"
- category_id: 5
- created_at: "2025-02-06 10:30:00"

┌────────────────────────────────────────────────────────┐
│ گام ۵: ارسال جواب به فرانت‌اند                        │
└────────────────────────────────────────────────────────┘
بک‌اند جواب برمی‌گردونه:
Status: 201 Created
Body: {
  id: 456,
  name: "کباب کوبیده",
  price: 250000,
  ...
}

┌────────────────────────────────────────────────────────┐
│ گام ۶: نمایش در فرانت‌اند                             │
└────────────────────────────────────────────────────────┘
فرانت‌اند پیغام موفقیت نشون میده:
"✓ کباب کوبیده با موفقیت اضافه شد"

لیست منو رو دوباره دریافت می‌کنه و نمایش میده
```

---

## 🎯 خلاصه نهایی با زبان خیلی ساده

### **دیتابیس (PostgreSQL):**
```
یک دیتابیس بزرگ با جداول مختلف
├── جداول مشترک (همه ازشون استفاده می‌کنن)
└── جداول اختصاصی (هر کدوم برای یک نوع سایت)

نکته مهم: همه جدول‌ها یک ستون site_id دارن که مشخص می‌کنه 
این داده متعلق به کدوم سایته
```

### **بک‌اند (Django):**
```
یک سرور که شامل بخش‌های مختلف (Apps) هست
├── بخش اصلی (Core): کاربران، سایت‌ها، تم‌ها
├── بخش بلاگ (Blog Module): مدیریت پست‌ها
├── بخش رستوران (Restaurant Module): مدیریت منو
└── بخش فروشگاه (Ecommerce Module): مدیریت محصولات

هر بخش کارش رو مستقل انجام میده ولی همه با هم هماهنگ کار می‌کنن
```

### **فرانت‌اند (Next.js):**
```
یک داشبورد هوشمند که بسته به نوع سایت، منو و صفحاتش عوض میشه

مثلاً:
- اگر سایت بلاگ باشه → بخش پست‌ها رو نشون میده
- اگر سایت رستوران باشه → بخش منو رو نشون میده
- اگر سایت فروشگاه باشه → بخش محصولات رو نشون میده

✅ چرا این معماری خوبه؟
۱. همه چیز در یک جا (ولی منظم)

یک بک‌اند، یک دیتابیس، یک فرانت‌اند
نگهداری راحت
هزینه کم

۲. راحت میشه چیز جدید اضافه کرد
می‌خوای نوع سایت جدید اضافه کنی؟

یک App جدید در Django بساز
یک ماژول جدید در فرانت‌اند اضافه کن
تموم!

۳. داده‌ها قاطی نمیشن
چون همه جا از site_id استفاده می‌کنیم، هر سایت فقط داده‌های خودش رو میبینه
۴. سریع میشه شروع کرد
Django خیلی چیزهای آماده داره، نیازی نیست از صفر بنویسی
۵. وقتی بزرگ شد، میشه تغییرش داد
اگه بعداً یک بخش خیلی شلوغ شد، میشه اون بخش رو جدا کرد و روی یک سرور مجزا گذاشت

🚀 از کجا شروع کنیم؟
گام ۱: بساز دیتابیس و جداول اصلی رو
گام ۲: بساز API های پایه رو (کاربر، سایت)
گام ۳: بساز فرانت‌اند پایه رو (لاگین، داشبورد)
گام ۴: بساز اولین ماژول رو (مثلاً رستوران - ساده‌ترینه)
گام ۵: تست کن، مطمئن شو کار می‌کنه
گام ۶: اضافه کن ماژول‌های بعدی رو (بلاگ، فروشگاه، ...)
این معماری بهت اجازه میده آروم پیش بری و هر قدم رو محکم بزاری. موفق باشی! 🎉خیلی خب بدونکد نوشتن یه فایل d بنویس که برای هر بخش از این پلتفرم توش توضیح بدی از چه روشی و چه ساختار و معماری استفاده بشه و چه ابزاری تا بهترین زیرساخت وعملکرد و معماریی رو داشتهباشیم3:38 PM📘 راهنمای جامع معماری Platform Site Builder

📑 فهرست مطالب

نگاه کلی به پروژه
معماری کلی سیستم
بخش دیتابیس
بخش بک‌اند
بخش فرانت‌اند
سیستم احراز هویت و امنیت
مدیریت فایل و رسانه
سیستم Page Builder
سیستم پرداخت و اشتراک
Analytics و گزارش‌گیری
Background Jobs و صف پیام
Cache و بهینه‌سازی عملکرد
Deployment و زیرساخت
Monitoring و نظارت
مقیاس‌پذیری و آینده پروژه


🎯 بخش ۱: نگاه کلی به پروژه {#overview}
هدف پروژه
یک SaaS Platform برای ساخت و مدیریت انواع مختلف وب‌سایت‌ها:

🍽️ رستوران و کافه
📝 بلاگ و وبلاگ شخصی
🛒 فروشگاه آنلاین
💼 پورتفولیو و نمونه کار
🏢 سایت شرکتی

ویژگی‌های کلیدی

Multi-tenant (هر کاربر چندین سایت)
Page Builder بصری (Drag & Drop)
سیستم Template و Theme
Dashboard اختصاصی برای هر نوع سایت
مدیریت Subscription
Analytics و گزارش‌گیری


🏗️ بخش ۲: معماری کلی سیستم {#system-architecture}
الگوی معماری: Modular Monolith
چرا Monolith و نه Microservices؟
مزایای Monolith در مرحله اولیه:

توسعه سریع‌تر
Debug و Test راحت‌تر
هزینه DevOps کمتر
Deployment ساده‌تر
Latency کمتر (بدون Network Call)

زمان مناسب برای Microservices:

وقتی به 100k+ کاربر فعال رسیدید
وقتی تیم بزرگ‌تر شد (10+ نفر)
وقتی بخش خاصی bottleneck شد

معماری لایه‌ای (Layered Architecture)
┌───────────────────────────────────────────────┐
│         Presentation Layer (Frontend)         │
│              Next.js 15 + React               │
└───────────────────────────────────────────────┘
                      ↕ HTTP/REST
┌───────────────────────────────────────────────┐
│          API Gateway Layer (Django)           │
│        Authentication & Rate Limiting          │
└───────────────────────────────────────────────┘
                      ↕
┌───────────────────────────────────────────────┐
│        Business Logic Layer (Django)          │
│   ┌─────────┬─────────┬──────────────────┐   │
│   │  Core   │  Blog   │  Restaurant/...  │   │
│   │ Module  │ Module  │     Modules      │   │
│   └─────────┴─────────┴──────────────────┘   │
└───────────────────────────────────────────────┘
                      ↕
┌───────────────────────────────────────────────┐
│         Data Access Layer (ORM)               │
│            Django ORM + Managers              │
└───────────────────────────────────────────────┘
                      ↕
┌───────────────────────────────────────────────┐
│         Data Storage Layer                    │
│   PostgreSQL + Redis + S3-Compatible          │
└───────────────────────────────────────────────┘
نکات کلیدی معماری:
Separation of Concerns:

هر لایه مسئولیت مشخص داره
تغییر در یک لایه، بقیه رو تحت تاثیر قرار نمیده

Dependency Rule:

لایه‌های بالایی به پایینی وابسته‌ان (نه برعکس)
Business Logic نباید به Framework وابسته باشه

Interface Segregation:

API های کوچک و مشخص
هر Module interface خودش رو export می‌کنه


💾 بخش ۳: دیتابیس {#database}
تکنولوژی: PostgreSQL 15+
چرا PostgreSQL؟
مزایا:

✅ ACID کامل (تضمین یکپارچگی داده)
✅ JSON/JSONB برای داده‌های پویا
✅ Full-text Search داخلی
✅ Row-level Security برای Multi-tenant
✅ Partitioning برای جداول بزرگ
✅ Extension‌های قدرتمند (PostGIS, pgvector)
✅ Performance عالی
✅ رایگان و Open Source

مقایسه با گزینه‌های دیگر:
ویژگیPostgreSQLMySQLMongoDBACID✅ کامل⚠️ محدود❌ ضعیفJSON Support✅ Native⚠️ محدود✅ NativeComplex Queries✅ عالی⚠️ خوب❌ ضعیفFull-text Search✅ داخلی⚠️ خارجی✅ داخلیMulti-tenant✅ Row-level⚠️ دستی⚠️ دستی

الگوی Multi-Tenancy: Shared Database + Tenant Isolation
سه الگوی اصلی Multi-tenant:
۱. Shared Database, Shared Schema:

همه در یک دیتابیس و یک Schema
تفکیک با tenant_id در هر جدول
✅ مناسب برای این پروژه

۲. Shared Database, Separate Schema:

هر tenant یک Schema جدا
❌ پیچیده برای این پروژه

۳. Separate Database:

هر tenant یک دیتابیس
❌ خیلی پیچیده و گران

ساختار جداول اصلی
جداول Core (مشترک):
users
├── id (UUID, PK)
├── email (UNIQUE)
├── password_hash
├── full_name
├── is_active
├── is_verified
├── created_at
└── updated_at

sites
├── id (UUID, PK)
├── user_id (FK → users)
├── site_type (ENUM: blog, restaurant, ecommerce, portfolio, corporate)
├── name
├── subdomain (UNIQUE)
├── custom_domain (UNIQUE, NULLABLE)
├── theme_id (FK → themes)
├── settings (JSONB) ← رنگ‌ها، فونت‌ها، لوگو
├── is_active
├── published_at (NULLABLE)
├── created_at
└── updated_at

themes
├── id (UUID, PK)
├── name
├── slug (UNIQUE)
├── category (ENUM: restaurant, blog, ecommerce, all)
├── preview_image
├── config (JSONB) ← تنظیمات پیش‌فرض تم
├── is_premium
├── price
└── created_at

pages
├── id (UUID, PK)
├── site_id (FK → sites)
├── title
├── slug
├── is_home
├── content (JSONB) ← داده‌های Page Builder
├── seo_title
├── seo_description
├── seo_keywords
├── is_published
├── published_at
├── created_at
└── updated_at

media_files
├── id (UUID, PK)
├── site_id (FK → sites)
├── file_name
├── file_path
├── file_type (ENUM: image, video, document)
├── mime_type
├── file_size
├── width (NULLABLE)
├── height (NULLABLE)
├── alt_text
├── uploaded_at
└── created_at

subscriptions
├── id (UUID, PK)
├── user_id (FK → users)
├── plan (ENUM: free, basic, pro, enterprise)
├── status (ENUM: active, canceled, expired, trial)
├── trial_ends_at
├── current_period_start
├── current_period_end
├── cancel_at_period_end
└── created_at
جداول Feature-Specific:
blog_posts
├── id (UUID, PK)
├── site_id (FK → sites) ← فیلتر کلیدی
├── title
├── slug
├── content (JSONB) ← Editor.js format
├── excerpt
├── featured_image
├── author_id (FK → users)
├── category_id (FK → blog_categories)
├── tags (ARRAY)
├── status (ENUM: draft, published, scheduled)
├── view_count
├── published_at
├── created_at
└── updated_at

blog_categories
├── id (UUID, PK)
├── site_id (FK → sites)
├── name
├── slug
├── description
├── parent_id (FK → blog_categories, NULLABLE)
└── created_at

blog_comments
├── id (UUID, PK)
├── post_id (FK → blog_posts)
├── author_name
├── author_email
├── content
├── status (ENUM: pending, approved, spam)
├── parent_id (FK → blog_comments, NULLABLE)
└── created_at

menu_items
├── id (UUID, PK)
├── site_id (FK → sites)
├── name
├── description
├── price
├── category_id (FK → menu_categories)
├── image
├── dietary_info (ARRAY: vegetarian, vegan, gluten-free)
├── allergens (ARRAY)
├── is_available
├── sort_order
└── created_at

menu_categories
├── id (UUID, PK)
├── site_id (FK → sites)
├── name
├── description
├── sort_order
└── created_at

products
├── id (UUID, PK)
├── site_id (FK → sites)
├── name
├── slug
├── description
├── sku (UNIQUE)
├── price
├── compare_price
├── cost_price
├── images (ARRAY)
├── category_id (FK → product_categories)
├── inventory_quantity
├── track_inventory
├── variants (JSONB) ← size, color, etc.
├── is_published
└── created_at

orders
├── id (UUID, PK)
├── site_id (FK → sites)
├── order_number (UNIQUE)
├── customer_email
├── customer_name
├── shipping_address (JSONB)
├── billing_address (JSONB)
├── items (JSONB)
├── subtotal
├── tax
├── shipping_cost
├── total
├── status (ENUM: pending, paid, shipped, completed, canceled)
├── payment_status (ENUM: unpaid, paid, refunded)
├── payment_method
├── payment_id
├── created_at
└── updated_at

portfolio_projects
├── id (UUID, PK)
├── site_id (FK → sites)
├── title
├── slug
├── description
├── content (JSONB)
├── client_name
├── project_date
├── featured_image
├── gallery (ARRAY)
├── technologies (ARRAY)
├── project_url
├── is_featured
└── created_at

استراتژی Indexing
Index‌های ضروری:
sql-- Tenant Isolation (خیلی مهم!)
CREATE INDEX idx_blog_posts_site_id ON blog_posts(site_id);
CREATE INDEX idx_menu_items_site_id ON menu_items(site_id);
CREATE INDEX idx_products_site_id ON products(site_id);

-- Lookup سریع
CREATE INDEX idx_sites_subdomain ON sites(subdomain);
CREATE INDEX idx_sites_custom_domain ON sites(custom_domain);
CREATE INDEX idx_users_email ON users(email);

-- Filtering و Sorting
CREATE INDEX idx_blog_posts_status_published ON blog_posts(status, published_at DESC);
CREATE INDEX idx_products_published ON products(site_id, is_published);

-- Full-text Search
CREATE INDEX idx_blog_posts_search ON blog_posts USING GIN(to_tsvector('english', title || ' ' || content));

استراتژی Partitioning (برای آینده)
زمانی که داده‌ها زیاد شد:
sql-- Partition by site_id (Hash Partitioning)
CREATE TABLE blog_posts (
    ...
) PARTITION BY HASH (site_id);

CREATE TABLE blog_posts_p0 PARTITION OF blog_posts
    FOR VALUES WITH (MODULUS 4, REMAINDER 0);
    
CREATE TABLE blog_posts_p1 PARTITION OF blog_posts
    FOR VALUES WITH (MODULUS 4, REMAINDER 1);
-- ...
```

---

### استراتژی Backup

**روزانه:**
- Full Backup در ساعت کم‌کاربر (3 صبح)
- ذخیره در S3 با Encryption
- نگهداری 30 روز اخیر

**هفتگی:**
- Full Backup + Verification
- ذخیره در مکان جغرافیایی مختلف
- نگهداری 3 ماه اخیر

**ماهانه:**
- Archive Backup
- نگهداری 1 سال

**ابزار پیشنهادی:**
- `pg_dump` برای Full Backup
- `pgBackRest` برای Incremental Backup
- AWS RDS Automated Backups (اگر از RDS استفاده کردید)

---

## ⚙️ بخش ۴: بک‌اند {#backend}

### تکنولوژی: **Django 5.0+ با Python 3.11+**

#### چرا Django؟

**مزایای اصلی:**
- ✅ Batteries Included (همه چیز آماده)
- ✅ ORM قدرتمند
- ✅ Admin Panel رایگان
- ✅ Security built-in
- ✅ سرعت توسعه بالا
- ✅ Ecosystem غنی
- ✅ مستندات عالی

---

### ساختار Project Django
```
backend/
├── config/                      # تنظیمات اصلی پروژه
│   ├── __init__.py
│   ├── settings/
│   │   ├── __init__.py
│   │   ├── base.py              # تنظیمات مشترک
│   │   ├── development.py       # محیط توسعه
│   │   ├── production.py        # محیط Production
│   │   └── test.py              # محیط تست
│   ├── urls.py                  # URL routing اصلی
│   ├── wsgi.py                  # WSGI config
│   └── asgi.py                  # ASGI config (برای WebSocket)
│
├── apps/                        # Django Apps
│   ├── core/                    # App اصلی (مشترک)
│   │   ├── __init__.py
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── site.py
│   │   │   ├── theme.py
│   │   │   ├── page.py
│   │   │   └── media.py
│   │   ├── managers/            # Custom Model Managers
│   │   │   ├── __init__.py
│   │   │   └── site_manager.py
│   │   ├── services/            # Business Logic
│   │   │   ├── __init__.py
│   │   │   ├── site_service.py
│   │   │   └── theme_service.py
│   │   ├── admin.py
│   │   ├── apps.py
│   │   └── tests/
│   │
│   ├── authentication/          # احراز هویت
│   │   ├── models.py
│   │   ├── services/
│   │   │   ├── auth_service.py
│   │   │   └── email_verification.py
│   │   ├── serializers.py
│   │   ├── views.py
│   │   └── urls.py
│   │
│   ├── blog/                    # ماژول بلاگ
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   ├── post.py
│   │   │   ├── category.py
│   │   │   └── comment.py
│   │   ├── services/
│   │   │   ├── post_service.py
│   │   │   └── seo_service.py
│   │   ├── serializers.py
│   │   ├── views.py
│   │   ├── admin.py
│   │   └── urls.py
│   │
│   ├── restaurant/              # ماژول رستوران
│   │   ├── models/
│   │   │   ├── menu_item.py
│   │   │   └── category.py
│   │   ├── services/
│   │   │   ├── menu_service.py
│   │   │   └── qr_generator.py
│   │   ├── serializers.py
│   │   ├── views.py
│   │   └── urls.py
│   │
│   ├── ecommerce/               # ماژول فروشگاه
│   │   ├── models/
│   │   │   ├── product.py
│   │   │   ├── order.py
│   │   │   └── cart.py
│   │   ├── services/
│   │   │   ├── product_service.py
│   │   │   ├── order_service.py
│   │   │   └── payment_service.py
│   │   ├── serializers.py
│   │   ├── views.py
│   │   └── urls.py
│   │
│   ├── subscription/            # مدیریت اشتراک
│   │   ├── models.py
│   │   ├── services/
│   │   │   ├── subscription_service.py
│   │   │   └── billing_service.py
│   │   ├── serializers.py
│   │   └── views.py
│   │
│   ├── analytics/               # آمار و گزارش
│   │   ├── models.py
│   │   ├── services/
│   │   │   └── analytics_service.py
│   │   └── views.py
│   │
│   └── builder/                 # Page Builder
│       ├── models.py
│       ├── services/
│       │   ├── block_validator.py
│       │   └── render_service.py
│       ├── serializers.py
│       └── views.py
│
├── api/                         # API Layer
│   ├── __init__.py
│   ├── v1/
│   │   ├── __init__.py
│   │   ├── urls.py              # API URLs
│   │   ├── routers.py           # DRF Routers
│   │   └── permissions.py       # Custom Permissions
│   └── middleware/
│       ├── tenant_middleware.py
│       └── rate_limit.py
│
├── utils/                       # توابع کمکی
│   ├── __init__.py
│   ├── validators.py
│   ├── helpers.py
│   ├── exceptions.py
│   └── constants.py
│
├── tasks/                       # Celery Tasks
│   ├── __init__.py
│   ├── email_tasks.py
│   ├── media_tasks.py
│   └── analytics_tasks.py
│
├── tests/                       # تست‌های Integration
│   ├── __init__.py
│   ├── test_api.py
│   └── test_services.py
│
├── manage.py
├── requirements/
│   ├── base.txt
│   ├── development.txt
│   ├── production.txt
│   └── test.txt
└── pytest.ini

الگوی Service Layer
چرا Service Layer؟
Views نباید Business Logic داشته باشن. این کار رو به Service ها بسپارید.
مثال:
python# ❌ بد: Business Logic در View
class PostCreateView(APIView):
    def post(self, request):
        # منطق پیچیده در View
        post = Post.objects.create(...)
        # ارسال ایمیل
        # آپدیت آمار
        # ...

# ✅ خوب: استفاده از Service
class PostCreateView(APIView):
    def post(self, request):
        serializer = PostSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)
        
        post = PostService.create_post(
            site=request.site,
            user=request.user,
            **serializer.validated_data
        )
        
        return Response(PostSerializer(post).data)

# apps/blog/services/post_service.py
class PostService:
    @staticmethod
    def create_post(site, user, **data):
        post = Post.objects.create(
            site=site,
            author=user,
            **data
        )
        
        # Business Logic
        EmailService.send_new_post_notification(post)
        AnalyticsService.track_post_created(site, post)
        
        return post

الگوی Repository (اختیاری ولی توصیه میشه)
برای جداسازی کامل‌تر Data Access:
python# apps/blog/repositories/post_repository.py
class PostRepository:
    @staticmethod
    def get_published_posts(site_id):
        return Post.objects.filter(
            site_id=site_id,
            status='published',
            published_at__lte=timezone.now()
        ).select_related('author', 'category')
    
    @staticmethod
    def get_by_slug(site_id, slug):
        return Post.objects.get(site_id=site_id, slug=slug)
```

---

### API Design: RESTful با Django REST Framework

**ساختار URL:**
```
/api/v1/
├── auth/
│   ├── POST   /register
│   ├── POST   /login
│   ├── POST   /logout
│   ├── POST   /refresh
│   ├── POST   /verify-email
│   ├── POST   /forgot-password
│   └── POST   /reset-password
│
├── users/
│   ├── GET    /me
│   └── PATCH  /me
│
├── sites/
│   ├── GET    /               (لیست سایت‌های کاربر)
│   ├── POST   /               (ساخت سایت جدید)
│   ├── GET    /{site_id}/
│   ├── PATCH  /{site_id}/
│   ├── DELETE /{site_id}/
│   └── GET    /{site_id}/analytics/
│
├── themes/
│   ├── GET    /               (لیست تم‌های موجود)
│   └── GET    /{theme_id}/
│
├── pages/
│   ├── GET    /sites/{site_id}/pages/
│   ├── POST   /sites/{site_id}/pages/
│   ├── GET    /sites/{site_id}/pages/{page_id}/
│   ├── PATCH  /sites/{site_id}/pages/{page_id}/
│   └── DELETE /sites/{site_id}/pages/{page_id}/
│
├── media/
│   ├── POST   /sites/{site_id}/upload/
│   ├── GET    /sites/{site_id}/media/
│   └── DELETE /sites/{site_id}/media/{media_id}/
│
├── blog/
│   ├── GET    /sites/{site_id}/posts/
│   ├── POST   /sites/{site_id}/posts/
│   ├── GET    /sites/{site_id}/posts/{post_id}/
│   ├── PATCH  /sites/{site_id}/posts/{post_id}/
│   ├── DELETE /sites/{site_id}/posts/{post_id}/
│   │
│   ├── GET    /sites/{site_id}/categories/
│   ├── POST   /sites/{site_id}/categories/
│   │
│   └── GET    /sites/{site_id}/posts/{post_id}/comments/
│
├── restaurant/
│   ├── GET    /sites/{site_id}/menu/
│   ├── POST   /sites/{site_id}/menu/
│   ├── PATCH  /sites/{site_id}/menu/{item_id}/
│   ├── DELETE /sites/{site_id}/menu/{item_id}/
│   │
│   ├── GET    /sites/{site_id}/categories/
│   └── GET    /sites/{site_id}/qr-code/
│
└── ecommerce/
    ├── GET    /sites/{site_id}/products/
    ├── POST   /sites/{site_id}/products/
    ├── GET    /sites/{site_id}/orders/
    └── POST   /sites/{site_id}/orders/

Permissions و Authorization
سطوح دسترسی:
python# api/v1/permissions.py

class IsSiteOwner(BasePermission):
    """
    فقط مالک سایت اجازه دسترسی داره
    """
    def has_object_permission(self, request, view, obj):
        if hasattr(obj, 'site'):
            return obj.site.user == request.user
        return obj.user == request.user

class IsSiteOwnerOrReadOnly(BasePermission):
    """
    مالک سایت می‌تونه تغییر بده، بقیه فقط مشاهده
    """
    def has_object_permission(self, request, view, obj):
        if request.method in SAFE_METHODS:
            return True
        return obj.site.user == request.user

class HasActiveSubscription(BasePermission):
    """
    کاربر باید اشتراک فعال داشته باشه
    """
    def has_permission(self, request, view):
        return request.user.subscription.is_active

Middleware برای Tenant Isolation
python# api/middleware/tenant_middleware.py

class TenantMiddleware:
    """
    site_id رو از URL استخراج می‌کنه و به request اضافه می‌کنه
    """
    def __init__(self, get_response):
        self.get_response = get_response
    
    def __call__(self, request):
        # استخراج site_id از URL
        if 'site_id' in request.resolver_match.kwargs:
            site_id = request.resolver_match.kwargs['site_id']
            
            try:
                site = Site.objects.get(
                    id=site_id,
                    user=request.user  # اطمینان از مالکیت
                )
                request.site = site
            except Site.DoesNotExist:
                return JsonResponse(
                    {'error': 'Site not found'},
                    status=404
                )
        
        response = self.get_response(request)
        return response

Rate Limiting
استراتژی:
python# تنظیمات
REST_FRAMEWORK = {
    'DEFAULT_THROTTLE_CLASSES': [
        'rest_framework.throttling.AnonRateThrottle',
        'rest_framework.throttling.UserRateThrottle'
    ],
    'DEFAULT_THROTTLE_RATES': {
        'anon': '100/hour',      # کاربر مهمان
        'user': '1000/hour',     # کاربر لاگین
        'upload': '50/hour',     # آپلود فایل
        'payment': '10/hour',    # پرداخت
    }
}

# Custom Throttle
class UploadRateThrottle(UserRateThrottle):
    scope = 'upload'

Validation
استفاده از Django Forms و DRF Serializers:
python# apps/blog/serializers.py

class PostSerializer(serializers.ModelSerializer):
    class Meta:
        model = Post
        fields = ['id', 'title', 'slug', 'content', ...]
        read_only_fields = ['id', 'created_at']
    
    def validate_slug(self, value):
        """اطمینان از unique بودن slug در هر سایت"""
        site = self.context['request'].site
        
        if Post.objects.filter(
            site=site,
            slug=value
        ).exclude(pk=self.instance.pk if self.instance else None).exists():
            raise serializers.ValidationError(
                "این slug قبلاً استفاده شده"
            )
        
        return value
    
    def validate_content(self, value):
        """اعتبارسنجی ساختار JSON محتوا"""
        try:
            # چک کردن فرمت Editor.js
            if not isinstance(value, dict):
                raise ValueError
            if 'blocks' not in value:
                raise ValueError
        except:
            raise serializers.ValidationError(
                "فرمت محتوا نامعتبر است"
            )
        
        return value

Error Handling
رویکرد یکپارچه:
python# utils/exceptions.py

class BaseAPIException(APIException):
    """Base class برای همه exception های سفارشی"""
    pass

class SiteNotFoundException(BaseAPIException):
    status_code = 404
    default_detail = 'سایت یافت نشد'

class SubscriptionExpiredException(BaseAPIException):
    status_code = 403
    default_detail = 'اشتراک شما منقضی شده است'

# Handler سفارشی
def custom_exception_handler(exc, context):
    response = exception_handler(exc, context)
    
    if response is not None:
        response.data = {
            'error': {
                'message': response.data.get('detail', 'خطایی رخ داد'),
                'code': exc.get_codes() if hasattr(exc, 'get_codes') else None,
                'status': response.status_code
            }
        }
    
    return response
```

---

## 🎨 بخش ۵: فرانت‌اند {#frontend}

### تکنولوژی: **Next.js 15 (App Router) + React 19**

#### چرا Next.js؟

**مزایا:**
- ✅ SSR و SSG برای SEO
- ✅ File-based Routing
- ✅ API Routes (اختیاری)
- ✅ Image Optimization خودکار
- ✅ Code Splitting خودکار
- ✅ TypeScript Support عالی
- ✅ Performance بالا

---

### ساختار Project Next.js
```
frontend/
├── app/                         # App Router
│   ├── (public)/                # Layout عمومی (بدون auth)
│   │   ├── layout.tsx
│   │   ├── page.tsx             # صفحه اصلی (Landing)
│   │   ├── pricing/
│   │   │   └── page.tsx
│   │   ├── templates/
│   │   │   └── page.tsx
│   │   └── contact/
│   │       └── page.tsx
│   │
│   ├── (auth)/                  # Layout احراز هویت
│   │   ├── layout.tsx
│   │   ├── login/
│   │   │   └── page.tsx
│   │   ├── register/
│   │   │   └── page.tsx
│   │   ├── verify/
│   │   │   └── page.tsx
│   │   └── reset-password/
│   │       └── page.tsx
│   │
│   ├── dashboard/               # داشبورد اصلی
│   │   ├── layout.tsx           # Sidebar + Header
│   │   ├── page.tsx             # Overview
│   │   │
│   │   ├── sites/
│   │   │   ├── page.tsx         # لیست سایت‌ها
│   │   │   ├── new/
│   │   │   │   └── page.tsx     # ساخت سایت جدید
│   │   │   │
│   │   │   └── [siteId]/        # صفحات هر سایت
│   │   │       ├── layout.tsx   # Layout اختصاصی سایت
│   │   │       ├── page.tsx     # Overview سایت
│   │   │       │
│   │   │       ├── analytics/
│   │   │       │   └── page.tsx
│   │   │       │
│   │   │       ├── pages/       # مدیریت صفحات
│   │   │       │   ├── page.tsx
│   │   │       │   ├── new/
│   │   │       │   └── [pageId]/
│   │   │       │       └── edit/
│   │   │       │
│   │   │       ├── media/       # کتابخانه رسانه
│   │   │       │   └── page.tsx
│   │   │       │
│   │   │       ├── settings/    # تنظیمات
│   │   │       │   └── page.tsx
│   │   │       │
│   │   │       ├── blog/        # ماژول بلاگ
│   │   │       │   ├── posts/
│   │   │       │   │   ├── page.tsx
│   │   │       │   │   ├── new/
│   │   │       │   │   └── [postId]/
│   │   │       │   │       └── edit/
│   │   │       │   └── categories/
│   │   │       │
│   │   │       ├── menu/        # ماژول رستوران
│   │   │       │   ├── items/
│   │   │       │   │   ├── page.tsx
│   │   │       │   │   └── new/
│   │   │       │   ├── categories/
│   │   │       │   └── qr-code/
│   │   │       │
│   │   │       └── products/    # ماژول فروشگاه
│   │   │           ├── list/
│   │   │           ├── new/
│   │   │           └── orders/
│   │   │
│   │   ├── subscription/
│   │   │   └── page.tsx
│   │   │
│   │   └── settings/
│   │       └── page.tsx
│   │
│   ├── builder/                 # Page Builder
│   │   └── [siteId]/
│   │       └── [pageId]/
│   │           └── page.tsx
│   │
│   ├── layout.tsx               # Root Layout
│   └── globals.css
│
├── components/
│   ├── ui/                      # کامپوننت‌های پایه (shadcn/ui)
│   │   ├── button.tsx
│   │   ├── input.tsx
│   │   ├── card.tsx
│   │   ├── dialog.tsx
│   │   ├── dropdown.tsx
│   │   ├── table.tsx
│   │   └── ...
│   │
│   ├── layout/                  # کامپوننت‌های Layout
│   │   ├── Header.tsx
│   │   ├── Footer.tsx
│   │   ├── Sidebar.tsx
│   │   └── MobileMenu.tsx
│   │
│   ├── landing/                 # کامپوننت‌های Landing
│   │   ├── Hero.tsx
│   │   ├── Features.tsx
│   │   ├── Pricing.tsx
│   │   ├── Templates.tsx
│   │   ├── Testimonials.tsx
│   │   └── FAQ.tsx
│   │
│   ├── dashboard/               # کامپوننت‌های Dashboard
│   │   ├── DashboardSidebar.tsx
│   │   ├── DashboardHeader.tsx
│   │   ├── StatsCard.tsx
│   │   ├── ModuleLoader.tsx     # سیستم بارگذاری ماژول‌ها
│   │   └── ...
│   │
│   ├── modules/                 # کامپوننت‌های اختصاصی ماژول‌ها
│   │   ├── blog/
│   │   │   ├── PostEditor.tsx
│   │   │   ├── PostList.tsx
│   │   │   ├── CategorySelect.tsx
│   │   │   └── ...
│   │   │
│   │   ├── restaurant/
│   │   │   ├── MenuItemForm.tsx
│   │   │   ├── MenuItemCard.tsx
│   │   │   ├── QRCodeGenerator.tsx
│   │   │   └── ...
│   │   │
│   │   └── ecommerce/
│   │       ├── ProductForm.tsx
│   │       ├── ProductGrid.tsx
│   │       ├── OrderTable.tsx
│   │       └── ...
│   │
│   ├── builder/                 # کامپوننت‌های Page Builder
│   │   ├── Canvas.tsx
│   │   ├── BlockLibrary.tsx
│   │   ├── Inspector.tsx
│   │   ├── blocks/
│   │   │   ├── HeroBlock.tsx
│   │   │   ├── FeaturesBlock.tsx
│   │   │   ├── GalleryBlock.tsx
│   │   │   └── ...
│   │   └── ...
│   │
│   └── shared/                  # کامپوننت‌های مشترک
│       ├── LoadingSpinner.tsx
│       ├── ErrorBoundary.tsx
│       ├── EmptyState.tsx
│       └── ...
│
├── lib/
│   ├── api/                     # API Client
│   │   ├── client.ts            # Axios instance
│   │   ├── auth.ts
│   │   ├── sites.ts
│   │   ├── pages.ts
│   │   ├── blog.ts
│   │   ├── restaurant.ts
│   │   ├── ecommerce.ts
│   │   └── ...
│   │
│   ├── hooks/                   # Custom Hooks
│   │   ├── useAuth.ts
│   │   ├── useSite.ts
│   │   ├── useModules.ts
│   │   ├── useMediaUpload.ts
│   │   └── ...
│   │
│   ├── context/                 # React Context
│   │   ├── AuthContext.tsx
│   │   ├── SiteContext.tsx
│   │   └── ThemeContext.tsx
│   │
│   ├── config/
│   │   ├── modules.ts           # تنظیمات ماژول‌ها
│   │   ├── routes.ts
│   │   └── constants.ts
│   │
│   ├── utils/
│   │   ├── validators.ts
│   │   ├── formatters.ts
│   │   └── helpers.ts
│   │
│   └── types/                   # TypeScript Types
│       ├── api.ts
│       ├── site.ts
│       ├── modules.ts
│       └── ...
│
├── public/
│   ├── images/
│   ├── fonts/
│   └── icons/
│
├── styles/
│   └── globals.css
│
├── .env.local
├── .env.production
├── next.config.js
├── tailwind.config.ts
├── tsconfig.json
└── package.json

State Management Strategy
رویکرد پیشنهادی: ترکیب Context API + React Query
چرا نه Redux/Zustand؟

برای این پروژه اضافی‌ست
Server State با React Query مدیریت میشه
Client State کمه و Context کافیه

ساختار:
typescript// lib/context/AuthContext.tsx
import { createContext, useContext } from 'react';

interface AuthContextType {
  user: User | null;
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
  isLoading: boolean;
}

const AuthContext = createContext<AuthContextType | undefined>(undefined);

export function AuthProvider({ children }) {
  // منطق احراز هویت
  
  return (
    <AuthContext.Provider value={...}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth() {
  const context = useContext(AuthContext);
  if (!context) {
    throw new Error('useAuth must be used within AuthProvider');
  }
  return context;
}
React Query برای Server State:
typescript// lib/hooks/useSites.ts
import { useQuery, useMutation } from '@tanstack/react-query';
import { sitesApi } from '@/lib/api/sites';

export function useSites() {
  return useQuery({
    queryKey: ['sites'],
    queryFn: sitesApi.getAll,
  });
}

export function useCreateSite() {
  return useMutation({
    mutationFn: sitesApi.create,
    onSuccess: () => {
      // Invalidate cache
      queryClient.invalidateQueries(['sites']);
    },
  });
}

سیستم ماژول‌های پویا
فایل تنظیمات:
typescript// lib/config/modules.ts

export interface ModuleConfig {
  id: string;
  name: string;
  icon: LucideIcon;
  description: string;
  requiredSiteTypes: SiteType[];
  routes: {
    path: string;
    label: string;
    icon?: LucideIcon;
  }[];
  features?: {
    id: string;
    label: string;
    description: string;
    requiresPlan?: SubscriptionPlan;
  }[];
}

export const MODULES: Record<string, ModuleConfig> = {
  blog: {
    id: 'blog',
    name: 'بلاگ',
    icon: FileText,
    description: 'مدیریت پست‌ها و محتوای بلاگ',
    requiredSiteTypes: ['blog'],
    routes: [
      { path: 'posts', label: 'پست‌ها', icon: FileText },
      { path: 'categories', label: 'دسته‌بندی‌ها', icon: FolderOpen },
      { path: 'comments', label: 'کامنت‌ها', icon: MessageSquare },
    ],
    features: [
      {
        id: 'advanced-editor',
        label: 'ادیتور پیشرفته',
        description: 'ادیتور متن با قابلیت‌های پیشرفته',
        requiresPlan: 'pro',
      },
    ],
  },
  
  restaurant: {
    id: 'restaurant',
    name: 'رستوران',
    icon: UtensilsCrossed,
    description: 'مدیریت منو و خدمات رستوران',
    requiredSiteTypes: ['restaurant'],
    routes: [
      { path: 'menu', label: 'منو', icon: Menu },
      { path: 'categories', label: 'دسته‌بندی‌ها', icon: FolderOpen },
      { path: 'qr-code', label: 'کد QR', icon: QrCode },
    ],
  },
  
  ecommerce: {
    id: 'ecommerce',
    name: 'فروشگاه',
    icon: ShoppingCart,
    description: 'مدیریت محصولات و سفارشات',
    requiredSiteTypes: ['ecommerce'],
    routes: [
      { path: 'products', label: 'محصولات', icon: Package },
      { path: 'orders', label: 'سفارشات', icon: ShoppingBag },
      { path: 'inventory', label: 'موجودی', icon: Database },
    ],
  },
};

// Helper function
export function getAvailableModules(siteType: SiteType): ModuleConfig[] {
  return Object.values(MODULES).filter(module =>
    module.requiredSiteTypes.includes(siteType)
  );
}
استفاده در Sidebar:
typescript// components/dashboard/DashboardSidebar.tsx

export function DashboardSidebar({ site }: { site: Site }) {
  const availableModules = getAvailableModules(site.type);
  
  return (
    <aside>
      {/* منوهای پایه */}
      <nav>
        <NavItem href="analytics" icon={BarChart} label="آمار" />
        <NavItem href="pages" icon={FileText} label="صفحات" />
        <NavItem href="media" icon={Image} label="رسانه" />
      </nav>
      
      {/* منوهای ماژول‌ها */}
      {availableModules.map(module => (
        <ModuleMenu key={module.id} module={module} siteId={site.id} />
      ))}
      
      {/* منوی تنظیمات */}
      <nav>
        <NavItem href="settings" icon={Settings} label="تنظیمات" />
      </nav>
    </aside>
  );
}

Form Handling
استفاده از React Hook Form + Zod:
typescript// مثال: فرم ساخت پست

import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';

const postSchema = z.object({
  title: z.string().min(1, 'عنوان الزامی است').max(300),
  slug: z.string().regex(/^[a-z0-9-]+$/, 'فرمت slug نامعتبر'),
  content: z.object({
    blocks: z.array(z.any()),
  }),
  categoryId: z.string().uuid().optional(),
  status: z.enum(['draft', 'published', 'scheduled']),
});

type PostFormData = z.infer<typeof postSchema>;

export function PostForm() {
  const {
    register,
    handleSubmit,
    formState: { errors, isSubmitting },
  } = useForm<PostFormData>({
    resolver: zodResolver(postSchema),
  });
  
  const createPost = useCreatePost();
  
  const onSubmit = async (data: PostFormData) => {
    await createPost.mutateAsync(data);
  };
  
  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <Input {...register('title')} error={errors.title?.message} />
      <Input {...register('slug')} error={errors.slug?.message} />
      {/* ... */}
      <Button type="submit" loading={isSubmitting}>
        ذخیره
      </Button>
    </form>
  );
}

Performance Optimization
۱. Code Splitting:
typescript// Lazy load ماژول‌ها
const BlogModule = dynamic(() => import('@/components/modules/blog'));
const RestaurantModule = dynamic(() => import('@/components/modules/restaurant'));
۲. Image Optimization:
typescriptimport Image from 'next/image';

<Image
  src={post.featuredImage}
  alt={post.title}
  width={800}
  height={400}
  priority={false}
  loading="lazy"
/>
۳. Memo و useMemo:
typescriptconst MemoizedComponent = React.memo(ExpensiveComponent);

const sortedPosts = useMemo(() => {
  return posts.sort((a, b) => ...);
}, [posts]);
```

---

## 🔐 بخش ۶: احراز هویت و امنیت {#authentication}

### استراتژی: **JWT (JSON Web Tokens)**

#### ساختار:
```
Access Token:
- مدت اعتبار: 15 دقیقه
- ذخیره: Memory (State)
- استفاده: هر درخواست API

Refresh Token:
- مدت اعتبار: 7 روز
- ذخیره: HTTPOnly Cookie
- استفاده: Refresh کردن Access Token
```

#### چرا HTTPOnly Cookie برای Refresh Token؟

**مزایا:**
- ✅ ایمن در برابر XSS
- ✅ خودکار ارسال با هر Request
- ✅ کاربر نمی‌تونه دستکاری کنه

---

### Flow احراز هویت:
```
۱. ثبت‌نام/لاگین:
   User → Frontend → Backend
   ← Access Token (Response Body)
   ← Refresh Token (HTTPOnly Cookie)

۲. درخواست معمولی:
   Frontend → API (با Access Token در Header)
   ← Response

۳. Access Token منقضی شده:
   Frontend → API
   ← 401 Unauthorized
   
   Frontend → /auth/refresh (با Refresh Token Cookie)
   ← New Access Token
   
   Retry Original Request

۴. Logout:
   Frontend → /auth/logout
   ← Clear Cookies

پیاده‌سازی در Django:
python# استفاده از djangorestframework-simplejwt

SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(minutes=15),
    'REFRESH_TOKEN_LIFETIME': timedelta(days=7),
    'ROTATE_REFRESH_TOKENS': True,
    'BLACKLIST_AFTER_ROTATION': True,
    'AUTH_HEADER_TYPES': ('Bearer',),
}

# apps/authentication/views.py

class LoginView(APIView):
    def post(self, request):
        serializer = LoginSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)
        
        user = authenticate(
            email=serializer.validated_data['email'],
            password=serializer.validated_data['password']
        )
        
        if not user:
            return Response(
                {'error': 'اطلاعات نامعتبر'},
                status=400
            )
        
        # ساخت Token
        refresh = RefreshToken.for_user(user)
        
        response = Response({
            'access': str(refresh.access_token),
            'user': UserSerializer(user).data
        })
        
        # ذخیره Refresh Token در Cookie
        response.set_cookie(
            key='refresh_token',
            value=str(refresh),
            httponly=True,
            secure=True,  # فقط HTTPS
            samesite='Lax',
            max_age=7 * 24 * 60 * 60  # 7 روز
        )
        
        return response

پیاده‌سازی در Frontend:
typescript// lib/api/client.ts

import axios from 'axios';

const apiClient = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL,
  withCredentials: true,  // ارسال Cookies
});

// Interceptor برای اضافه کردن Access Token
apiClient.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('access_token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  }
);

// Interceptor برای Refresh کردن Token
apiClient.interceptors.response.use(
  (response) => response,
  async (error) => {
    const originalRequest = error.config;
    
    if (error.response?.status === 401 && !originalRequest._retry) {
      originalRequest._retry = true;
      
      try {
        // Refresh Token (Cookie خودکار ارسال میشه)
        const { data } = await axios.post(
          `${process.env.NEXT_PUBLIC_API_URL}/auth/refresh/`,
          {},
          { withCredentials: true }
        );
        
        // ذخیره Access Token جدید
        localStorage.setItem('access_token', data.access);
        
        // Retry درخواست اصلی
        originalRequest.headers.Authorization = `Bearer ${data.access}`;
        return apiClient(originalRequest);
      } catch (refreshError) {
        // Refresh هم ناموفق بود → Logout
        localStorage.removeItem('access_token');
        window.location.href = '/login';
        return Promise.reject(refreshError);
      }
    }
    
    return Promise.reject(error);
  }
);

امنیت اضافی:
۱. Rate Limiting:
python# محدودیت تلاش لاگین
'login': '5/hour',      # 5 تلاش در ساعت
'register': '3/hour',   # 3 ثبت‌نام در ساعت
۲. Email Verification:
python# کاربر تا verify نکرده نمی‌تونه کار کنه
class IsEmailVerified(BasePermission):
    def has_permission(self, request, view):
        return request.user.is_verified
۳. CSRF Protection:
python# Django پیش‌فرض CSRF داره
CSRF_COOKIE_HTTPONLY = True
CSRF_COOKIE_SECURE = True  # Production
۴. Password Policy:
pythonAUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
        'OPTIONS': {'min_length': 8}
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    # ...
]
```

---

## 📁 بخش ۷: مدیریت فایل و رسانه {#media}

### استراتژی: **S3-Compatible Object Storage**

#### تکنولوژی پیشنهادی: **CloudFlare R2**

**چرا CloudFlare R2؟**
- ✅ S3-Compatible API
- ✅ بدون هزینه Egress (ترافیک خروجی رایگان!)
- ✅ CDN داخلی و سریع
- ✅ قیمت مناسب‌تر از AWS S3
- ✅ Integration با CloudFlare Workers

**مقایسه:**

| ویژگی | R2 | S3 | DigitalOcean Spaces |
|------|---|---|---------------------|
| Storage (TB/month) | $15 | $23 | $5 (250GB) |
| Egress Traffic | رایگان | $90/TB | $10/TB |
| CDN | داخلی | CloudFront جدا | داخلی |

---

### ساختار ذخیره‌سازی:
```
bucket-name/
├── sites/
│   ├── {site_id}/
│   │   ├── media/
│   │   │   ├── {year}/
│   │   │   │   ├── {month}/
│   │   │   │   │   ├── {uuid}-{filename}.jpg
│   │   │   │   │   └── thumbnails/
│   │   │   │   │       ├── small_{uuid}-{filename}.jpg
│   │   │   │   │       ├── medium_{uuid}-{filename}.jpg
│   │   │   │   │       └── large_{uuid}-{filename}.jpg
│   │   ├── themes/
│   │   │   └── custom-assets/
│   │   └── exports/
│   │       └── backup-{date}.zip
└── system/
    ├── themes/
    │   └── preview-images/
    └── temp/

پیاده‌سازی Upload در Django:
python# settings.py

# استفاده از django-storages
STORAGES = {
    "default": {
        "BACKEND": "storages.backends.s3boto3.S3Boto3Storage",
        "OPTIONS": {
            "access_key": env('R2_ACCESS_KEY'),
            "secret_key": env('R2_SECRET_KEY'),
            "bucket_name": env('R2_BUCKET_NAME'),
            "endpoint_url": env('R2_ENDPOINT_URL'),
            "region_name": "auto",
            "file_overwrite": False,
        },
    },
}

# apps/core/services/media_service.py

from PIL import Image
from io import BytesIO
from django.core.files.uploadedfile import InMemoryUploadedFile

class MediaService:
    THUMBNAIL_SIZES = {
        'small': (300, 300),
        'medium': (800, 800),
        'large': (1200, 1200),
    }
    
    @classmethod
    def upload_image(cls, site, file, user):
        """
        آپلود تصویر و ساخت thumbnail ها
        """
        # Validation
        if file.size > 10 * 1024 * 1024:  # 10MB
            raise ValueError('حجم فایل بیش از حد مجاز')
        
        # ساخت path
        now = timezone.now()
        path = f"sites/{site.id}/media/{now.year}/{now.month:02d}/"
        filename = f"{uuid.uuid4()}-{file.name}"
        
        # ذخیره فایل اصلی
        full_path = default_storage.save(
            path + filename,
            file
        )
        
        # ساخت thumbnail ها (Async Task)
        generate_thumbnails_task.delay(full_path)
        
        # ذخیره در دیتابیس
        media_file = MediaFile.objects.create(
            site=site,
            file_name=file.name,
            file_path=full_path,
            file_type='image',
            mime_type=file.content_type,
            file_size=file.size,
        )
        
        return media_file
    
    @classmethod
    def generate_thumbnails(cls, file_path):
        """
        ساخت thumbnail (اجرا می‌شه در Celery)
        """
        # خواندن فایل از Storage
        file_content = default_storage.open(file_path).read()
        image = Image.open(BytesIO(file_content))
        
        # ساخت هر thumbnail
        for size_name, (width, height) in cls.THUMBNAIL_SIZES.items():
            # Resize
            image.thumbnail((width, height), Image.Resampling.LANCZOS)
            
            # ذخیره
            output = BytesIO()
            image.save(output, format='JPEG', quality=85, optimize=True)
            output.seek(0)
            
            # Path thumbnail
            thumb_path = file_path.replace(
                os.path.basename(file_path),
                f"thumbnails/{size_name}_{os.path.basename(file_path)}"
            )
            
            default_storage.save(thumb_path, output)

پیاده‌سازی Upload در Frontend:
typescript// lib/hooks/useMediaUpload.ts

export function useMediaUpload() {
  const [isUploading, setIsUploading] = useState(false);
  const [progress, setProgress] = useState(0);
  
  const upload = async (file: File, siteId: string) => {
    setIsUploading(true);
    setProgress(0);
    
    const formData = new FormData();
    formData.append('file', file);
    
    try {
      const { data } = await apiClient.post(
        `/api/sites/${siteId}/media/upload/`,
        formData,
        {
          headers: {
            'Content-Type': 'multipart/form-data',
          },
          onUploadProgress: (progressEvent) => {
            const percentCompleted = Math.round(
              (progressEvent.loaded * 100) / progressEvent.total!
            );
            setProgress(percentCompleted);
          },
        }
      );
      
      return data;
    } finally {
      setIsUploading(false);
      setProgress(0);
    }
  };
  
  return { upload, isUploading, progress };
}

// استفاده در کامپوننت
function ImageUploader() {
  const { upload, isUploading, progress } = useMediaUpload();
  const { siteId } = useSite();
  
  const handleUpload = async (e: ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    if (!file) return;
    
    try {
      const media = await upload(file, siteId);
      toast.success('فایل با موفقیت آپلود شد');
    } catch (error) {
      toast.error('خطا در آپلود فایل');
    }
  };
  
  return (
    <div>
      <input
        type="file"
        accept="image/*"
        onChange={handleUpload}
        disabled={isUploading}
      />
      {isUploading && <ProgressBar value={progress} />}
    </div>
  );
}

Image Optimization
در Frontend (Next.js):
typescript// استفاده از Next.js Image
import Image from 'next/image';

<Image
  src={media.file_path}
  alt={media.alt_text}
  width={800}
  height={600}
  sizes="(max-width: 768px) 100vw, 50vw"
  placeholder="blur"
  blurDataURL={media.blur_placeholder}
/>
در Backend (Celery Task):
python# tasks/media_tasks.py

@shared_task
def generate_thumbnails_task(file_path):
    MediaService.generate_thumbnails(file_path)

@shared_task
def optimize_image_task(file_path):
    """
    بهینه‌سازی تصویر با tinify یا pillow-simd
    """
    # کاهش حجم بدون کاهش کیفیت
    pass

🎨 بخش ۸: سیستم Page Builder {#page-builder}
رویکرد: Block-Based JSON Storage
چرا JSON؟
مزایا:

✅ انعطاف‌پذیری بالا
✅ تغییر ساختار راحت
✅ Version Control آسان
✅ PostgreSQL از JSONB خوب پشتیبانی می‌کنه


ساختار داده JSON:
json{
  "version": "1.0",
  "blocks": [
    {
      "id": "hero-1",
      "type": "hero",
      "order": 0,
      "data": {
        "title": "خوش آمدید",
        "subtitle": "بهترین رستوران شهر",
        "backgroundImage": "/media/hero-bg.jpg",
        "backgroundOverlay": 0.5,
        "ctaButtons": [
          {
            "text": "رزرو میز",
            "link": "/contact",
            "style": "primary"
          }
        ]
      },
      "style": {
        "backgroundColor": "#000",
        "textColor": "#fff",
        "textAlign": "center",
        "padding": { "top": 80, "bottom": 80 },
        "minHeight": 600
      }
    },
    {
      "id": "features-1",
      "type": "features",
      "order": 1,
      "data": {
        "title": "ویژگی‌های ما",
        "items": [
          {
            "icon": "chef-hat",
            "title": "سرآشپز حرفه‌ای",
            "description": "بهترین سرآشپزها"
          }
        ],
        "columns": 3
      },
      "style": {
        "backgroundColor": "#f5f5f5",
        "padding": { "top": 60, "bottom": 60 }
      }
    }
  ]
}
```

---

### کتابخانه بلوک‌ها:

**دسته‌بندی:**
```
Layout Blocks:
├── Hero (انواع مختلف)
├── Two Columns
├── Three Columns
└── Grid Layout

Content Blocks:
├── Text/Rich Text
├── Heading
├── Image
├── Video
└── Embed (YouTube, Google Maps, ...)

Media Blocks:
├── Image Gallery
├── Carousel/Slider
├── Video Gallery
└── Audio Player

Interactive Blocks:
├── Contact Form
├── Newsletter Signup
├── Button/CTA
├── Accordion/FAQ
└── Tabs

Data Display:
├── Pricing Table
├── Testimonials
├── Team Members
├── Stats/Numbers
└── Timeline

Module-Specific:
├── Blog Post List
├── Menu Display
├── Product Grid
└── Portfolio Gallery
```

---

### معماری Page Builder Frontend:
```
Page Builder Structure:
═══════════════════════════════════

┌─────────────────────────────────────┐
│           Top Toolbar               │
│  [Save] [Preview] [Publish] [←]    │
└─────────────────────────────────────┘

┌─────────┬──────────────────┬────────┐
│  Block  │                  │ Block  │
│ Library │      Canvas      │Settings│
│         │   (Preview)      │(Props) │
│         │                  │        │
│  Search │  ┌──────────┐    │ • Bg   │
│  ━━━━━  │  │  Block   │    │ • Text │
│         │  └──────────┘    │ • Pad  │
│ 📐Layout│  ┌──────────┐    │ • ...  │
│ • Hero  │  │  Block   │    │        │
│ • 2 Col │  └──────────┘    │[Delete]│
│         │  ┌──────────┐    │        │
│ 📝Content  │  Block   │    │        │
│ • Text  │  └──────────┘    │        │
│ • Image │                  │        │
│         │  [+ Add Block]   │        │
└─────────┴──────────────────┴────────┘

پیاده‌سازی در React:
typescript// types/builder.ts

export interface Block {
  id: string;
  type: string;
  order: number;
  data: Record<string, any>;
  style: BlockStyle;
}

export interface BlockStyle {
  backgroundColor?: string;
  textColor?: string;
  textAlign?: 'left' | 'center' | 'right';
  padding?: {
    top: number;
    right: number;
    bottom: number;
    left: number;
  };
  margin?: {
    top: number;
    right: number;
    bottom: number;
    left: number;
  };
  minHeight?: number;
  maxWidth?: number;
}

// components/builder/Canvas.tsx

export function Canvas() {
  const { blocks, updateBlock, deleteBlock, reorderBlocks } = useBuilder();
  const [selectedBlockId, setSelectedBlockId] = useState<string | null>(null);
  
  const sensors = useSensors(
    useSensor(PointerSensor),
    useSensor(KeyboardSensor)
  );
  
  const handleDragEnd = (event: DragEndEvent) => {
    const { active, over } = event;
    if (over && active.id !== over.id) {
      reorderBlocks(active.id, over.id);
    }
  };
  
  return (
    <DndContext sensors={sensors} onDragEnd={handleDragEnd}>
      <SortableContext items={blocks.map(b => b.id)}>
        <div className="canvas">
          {blocks.map(block => (
            <SortableBlock
              key={block.id}
              block={block}
              isSelected={selectedBlockId === block.id}
              onSelect={() => setSelectedBlockId(block.id)}
              onUpdate={updateBlock}
              onDelete={deleteBlock}
            />
          ))}
          
          <AddBlockButton />
        </div>
      </SortableContext>
    </DndContext>
  );
}

// components/builder/blocks/HeroBlock.tsx

export function HeroBlock({ data, style }: BlockProps) {
  return (
    <section
      className="hero-block"
      style={{
        backgroundColor: style.backgroundColor,
        color: style.textColor,
        minHeight: style.minHeight,
        textAlign: style.textAlign,
        ...
      }}
    >
      {data.backgroundImage && (
        <div className="hero-background">
          <Image src={data.backgroundImage} fill objectFit="cover" />
          <div
            className="overlay"
            style={{ opacity: data.backgroundOverlay }}
          />
        </div>
      )}
      
      <div className="hero-content">
        <h1>{data.title}</h1>
        {data.subtitle && <p>{data.subtitle}</p>}
        
        {data.ctaButtons?.map((btn, i) => (
          <Button key={i} variant={btn.style}>
            {btn.text}
          </Button>
        ))}
      </div>
    </section>
  );
}

Validation در Backend:
python# apps/builder/services/block_validator.py

from jsonschema import validate, ValidationError

BLOCK_SCHEMAS = {
    'hero': {
        'type': 'object',
        'required': ['title'],
        'properties': {
            'title': {'type': 'string', 'maxLength': 200},
            'subtitle': {'type': 'string', 'maxLength': 500},
            'backgroundImage': {'type': 'string'},
            'ctaButtons': {
                'type': 'array',
                'maxItems': 3,
                'items': {
                    'type': 'object',
                    'required': ['text'],
                    'properties': {
                        'text': {'type': 'string'},
                        'link': {'type': 'string'},
                        'style': {'enum': ['primary', 'secondary', 'outline']}
                    }
                }
            }
        }
    },
    # سایر بلوک‌ها...
}

class BlockValidator:
    @classmethod
    def validate_page_content(cls, content):
        """اعتبارسنجی کل محتوای صفحه"""
        if not isinstance(content, dict):
            raise ValueError('Content must be a dict')
        
        if 'blocks' not in content:
            raise ValueError('Content must have blocks')
        
        # اعتبارسنجی هر بلوک
        for block in content['blocks']:
            cls.validate_block(block)
    
    @classmethod
    def validate_block(cls, block):
        """اعتبارسنجی یک بلوک"""
        block_type = block.get('type')
        
        if block_type not in BLOCK_SCHEMAS:
            raise ValueError(f'Unknown block type: {block_type}')
        
        schema = BLOCK_SCHEMAS[block_type]
        
        try:
            validate(instance=block.get('data', {}), schema=schema)
        except ValidationError as e:
            raise ValueError(f'Invalid block data: {e.message}')
```

---

## 💳 بخش ۹: پرداخت و اشتراک {#payment}

### استراتژی: **Subscription-Based Pricing**

#### پلن‌های پیشنهادی:
```
Free Plan (رایگان):
├── 1 سایت
├── 5 صفحه
├── 100MB فضا
├── subdomain فقط
├── تم‌های رایگان
└── برندینگ پلتفرم

Basic Plan ($9/month):
├── 3 سایت
├── صفحات نامحدود
├── 5GB فضا
├── Custom Domain
├── همه تم‌ها
└── حذف برندینگ

Pro Plan ($29/month):
├── 10 سایت
├── صفحات نامحدود
├── 50GB فضا
├── Custom Domain
├── همه تم‌ها
├── E-commerce
├── Analytics پیشرفته
└── پشتیبانی Priority

Enterprise (سفارشی):
├── سایت نامحدود
├── فضای نامحدود
├── White Label
├── API Access
└── پشتیبانی اختصاصی

Payment Gateway: Stripe (بین‌المللی) + ZarinPal (ایران)
پیاده‌سازی Stripe:
python# apps/subscription/services/stripe_service.py

import stripe
from django.conf import settings

stripe.api_key = settings.STRIPE_SECRET_KEY

class StripeService:
    PRICE_IDS = {
        'basic_monthly': 'price_xxx',
        'basic_yearly': 'price_yyy',
        'pro_monthly': 'price_zzz',
        'pro_yearly': 'price_www',
    }
    
    @classmethod
    def create_checkout_session(cls, user, plan, billing_cycle):
        """
        ساخت Checkout Session برای پرداخت
        """
        price_id = cls.PRICE_IDS[f'{plan}_{billing_cycle}']
        
        session = stripe.checkout.Session.create(
            customer_email=user.email,
            payment_method_types=['card'],
            line_items=[{
                'price': price_id,
                'quantity': 1,
            }],
            mode='subscription',
            success_url=f'{settings.FRONTEND_URL}/dashboard/subscription?success=true',
            cancel_url=f'{settings.FRONTEND_URL}/dashboard/subscription?canceled=true',
            metadata={
                'user_id': str(user.id),
                'plan': plan,
            }
        )
        
        return session.url
    
    @classmethod
    def handle_webhook(cls, payload, sig_header):
        """
        پردازش Webhook های Stripe
        """
        try:
            event = stripe.Webhook.construct_event(
                payload,
                sig_header,
                settings.STRIPE_WEBHOOK_SECRET
            )
        except ValueError:
            raise ValueError('Invalid payload')
        except stripe.error.SignatureVerificationError:
            raise ValueError('Invalid signature')
        
        # پردازش رویدادها
        if event['type'] == 'checkout.session.completed':
            cls._handle_successful_payment(event['data']['object'])
        elif event['type'] == 'customer.subscription.deleted':
            cls._handle_subscription_canceled(event['data']['object'])
        # ...
    
    @classmethod
    def _handle_successful_payment(cls, session):
        """پرداخت موفق"""
        user_id = session['metadata']['user_id']
        plan = session['metadata']['plan']
        
        # آپدیت اشتراک کاربر
        subscription = Subscription.objects.get(user_id=user_id)
        subscription.plan = plan
        subscription.status = 'active'
        subscription.stripe_subscription_id = session['subscription']
        subscription.save()
        
        # ارسال ایمیل تایید
        EmailService.send_subscription_activated(subscription.user)

Subscription Lifecycle Management:
python# apps/subscription/services/subscription_service.py

class SubscriptionService:
    @classmethod
    def check_feature_access(cls, user, feature):
        """
        چک کردن دسترسی به یک ویژگی
        """
        subscription = user.subscription
        
        if not subscription.is_active:
            return False
        
        plan_features = {
            'free': ['basic_themes', 'subdomain'],
            'basic': ['basic_themes', 'custom_domain', 'analytics'],
            'pro': ['all_themes', 'ecommerce', 'advanced_analytics'],
        }
        
        return feature in plan_features.get(subscription.plan, [])
    
    @classmethod
    def check_limit(cls, user, resource):
        """
        چک کردن محدودیت منابع
        """
        subscription = user.subscription
        
        limits = {
            'free': {'sites': 1, 'pages_per_site': 5, 'storage_mb': 100},
            'basic': {'sites': 3, 'pages_per_site': None, 'storage_mb': 5000},
            'pro': {'sites': 10, 'pages_per_site': None, 'storage_mb': 50000},
        }
        
        plan_limits = limits[subscription.plan]
        
        if resource == 'sites':
            current = user.sites.count()
            return current < plan_limits['sites']
        
        # ...
    
    @classmethod
    def cancel_subscription(cls, user):
        """
        لغو اشتراک
        """
        subscription = user.subscription
        
        # لغو در Stripe
        if subscription.stripe_subscription_id:
            stripe.Subscription.modify(
                subscription.stripe_subscription_id,
                cancel_at_period_end=True
            )
        
        subscription.cancel_at_period_end = True
        subscription.save()

Usage Tracking:
python# Middleware برای تراک کردن استفاده

class UsageTrackingMiddleware:
    def process_request(self, request):
        if request.user.is_authenticated:
            # تراک API calls
            cache_key = f'api_calls:{request.user.id}:{today}'
            cache.incr(cache_key, 1)
            
            # چک محدودیت
            if cache.get(cache_key) > request.user.subscription.api_limit:
                return JsonResponse(
                    {'error': 'API limit exceeded'},
                    status=429
                )
```

---

## 📊 بخش ۱۰: Analytics و گزارش‌گیری {#analytics}

### استراتژی: **Self-Hosted Analytics**

#### چرا Self-Hosted؟

**مزایا:**
- ✅ کنترل کامل روی داده
- ✅ Privacy-Friendly
- ✅ هزینه صفر برای ترافیک
- ✅ Custom Events

**معایب:**
- ⚠️ باید خودت پیاده کنی
- ⚠️ نیاز به زیرساخت اضافه

---

### Metrics اصلی:
```
Site Analytics:
├── Pageviews (بازدید صفحات)
├── Unique Visitors (بازدیدکننده یکتا)
├── Bounce Rate (نرخ پرش)
├── Session Duration (مدت جلسه)
├── Traffic Sources (منابع ترافیک)
├── Popular Pages (محبوب‌ترین صفحات)
├── Geographic Data (کشور، شهر)
└── Device Data (دسکتاپ، موبایل)

Module-Specific:
├── Blog: Most Read Posts, Comments, Shares
├── Restaurant: Menu Views, Popular Items
└── E-commerce: Product Views, Cart Adds, Sales

پیاده‌سازی Tracking:
Frontend Tracking Script:
typescript// public/tracking.js (تزریق در سایت‌های کاربران)

(function() {
  const ANALYTICS_ENDPOINT = 'https://api.yourplatform.com/track';
  const SITE_ID = document.currentScript.dataset.siteId;
  
  // Session ID (ذخیره در localStorage)
  let sessionId = localStorage.getItem('session_id');
  if (!sessionId) {
    sessionId = generateUUID();
    localStorage.setItem('session_id', sessionId);
  }
  
  // Track Pageview
  function trackPageview() {
    const data = {
      site_id: SITE_ID,
      session_id: sessionId,
      event: 'pageview',
      url: window.location.href,
      referrer: document.referrer,
      screen: `${window.screen.width}x${window.screen.height}`,
      language: navigator.language,
      timestamp: Date.now(),
    };
    
    navigator.sendBeacon(ANALYTICS_ENDPOINT, JSON.stringify(data));
  }
  
  // Track on load
  trackPageview();
  
  // Track on SPA navigation
  let lastUrl = location.href;
  new MutationObserver(() => {
    if (location.href !== lastUrl) {
      lastUrl = location.href;
      trackPageview();
    }
  }).observe(document, { subtree: true, childList: true });
})();
Backend Processing:
python# apps/analytics/views.py

class TrackEventView(APIView):
    permission_classes = []  # Public endpoint
    
    def post(self, request):
        # Validation
        serializer = EventSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)
        
        # ذخیره در دیتابیس (Async via Celery)
        process_analytics_event.delay(serializer.validated_data)
        
        return Response(status=204)

# tasks/analytics_tasks.py

@shared_task
def process_analytics_event(event_data):
    """
    پردازش رویداد Analytics
    """
    # استخراج اطلاعات
    site_id = event_data['site_id']
    session_id = event_data['session_id']
    
    # چک Unique Visitor
    is_unique = not PageView.objects.filter(
        site_id=site_id,
        session_id=session_id,
        date=today
    ).exists()
    
    # ذخیره
    PageView.objects.create(
        site_id=site_id,
        session_id=session_id,
        url=event_data['url'],
        referrer=event_data.get('referrer'),
        is_unique=is_unique,
        user_agent=event_data.get('user_agent'),
        ip_address=anonymize_ip(event_data.get('ip')),
        timestamp=event_data['timestamp']
    )
    
    # آپدیت آمار روزانه (Aggregation)
    update_daily_stats.delay(site_id, today)

Aggregation برای گزارش‌های سریع:
python# models.py

class DailyStats(models.Model):
    """آمار روزانه برای نمایش سریع"""
    site = models.ForeignKey(Site, on_delete=models.CASCADE)
    date = models.DateField()
    pageviews = models.IntegerField(default=0)
    unique_visitors = models.IntegerField(default=0)
    bounce_rate = models.FloatField(default=0)
    avg_session_duration = models.IntegerField(default=0)
    
    class Meta:
        unique_together = ['site', 'date']
        indexes = [
            models.Index(fields=['site', 'date']),
        ]

# Task برای Aggregation

@shared_task
def update_daily_stats(site_id, date):
    """محاسبه آمار روزانه"""
    pageviews = PageView.objects.filter(
        site_id=site_id,
        date=date
    )
    
    stats, created = DailyStats.objects.get_or_create(
        site_id=site_id,
        date=date
    )
    
    stats.pageviews = pageviews.count()
    stats.unique_visitors = pageviews.filter(is_unique=True).count()
    # محاسبه bounce rate
    # محاسبه avg session duration
    stats.save()

⚙️ بخش ۱۱: Background Jobs {#background-jobs}
تکنولوژی: Celery + Redis
چرا Celery؟
مزایا:

✅ Integration عالی با Django
✅ پشتیبانی از Periodic Tasks
✅ Retry و Error Handling
✅ پشتیبانی از Priority
✅ Monitoring (Flower)


Setup:
python# config/celery.py

from celery import Celery
from celery.schedules import crontab

app = Celery('backend')
app.config_from_object('django.conf:settings', namespace='CELERY')
app.autodiscover_tasks()

# Periodic Tasks
app.conf.beat_schedule = {
    'generate-daily-reports': {
        'task': 'apps.analytics.tasks.generate_daily_reports',
        'schedule': crontab(hour=1, minute=0),  # هر روز ساعت 1 صبح
    },
    'check-expiring-subscriptions': {
        'task': 'apps.subscription.tasks.check_expiring_subscriptions',
        'schedule': crontab(hour=9, minute=0),  # هر روز ساعت 9 صبح
    },
    'cleanup-temp-files': {
        'task': 'apps.core.tasks.cleanup_temp_files',
        'schedule': crontab(hour=3, minute=0),  # هر روز ساعت 3 صبح
    },
}

Task Examples:
python# tasks/email_tasks.py

@shared_task(bind=True, max_retries=3)
def send_email_task(self, to, subject, template, context):
    """ارسال ایمیل (با retry در صورت خطا)"""
    try:
        send_mail(
            subject=subject,
            message=render_to_string(template, context),
            from_email=settings.DEFAULT_FROM_EMAIL,
            recipient_list=[to],
        )
    except Exception as exc:
        # Retry با Exponential Backoff
        raise self.retry(exc=exc, countdown=60 * (2 ** self.request.retries))

# tasks/media_tasks.py

@shared_task
def generate_thumbnails_task(media_id):
    """ساخت thumbnail برای تصویر"""
    media = MediaFile.objects.get(id=media_id)
    MediaService.generate_thumbnails(media.file_path)

@shared_task
def process_video_task(media_id):
    """پردازش ویدیو (فشرده‌سازی، thumbnail)"""
    # پردازش سنگین
    pass

# tasks/subscription_tasks.py

@shared_task
def check_expiring_subscriptions():
    """چک کردن اشتراک‌های در حال انقضا"""
    expiring_soon = Subscription.objects.filter(
        status='active',
        current_period_end__lte=timezone.now() + timedelta(days=7),
        current_period_end__gte=timezone.now()
    )
    
    for sub in expiring_soon:
        send_email_task.delay(
            to=sub.user.email,
            subject='اشتراک شما به زودی تمام می‌شود',
            template='emails/subscription_expiring.html',
            context={'subscription': sub}
        )

Task Priority: